---
title: "IMediaList.Item"
second_title: "Aspose.HTML for Java API リファレンス"
description: "IMediaList プロパティ。item(index) メソッドは、インデックスで指定されたメディアクエリのシリアライズを返すか、インデックスがコレクション内のメディアクエリ数以上の場合は null を返す必要があります。"
type: docs

url: /ja/java/com.aspose.html.dom.css/imedialist/item/
---
## IMediaList indexer

item(index) メソッドは、インデックスで指定されたメディアクエリのシリアライズを返すか、インデックスがコレクション内のメディアクエリ数以上の場合は null を返す必要があります。

```java
public String this[int index] { get; }
```

### 戻り値

[`MediaList`](../) 内のメディアクエリのインデックス値から、メディアクエリをテキストとして表す文字列を返します。

### Property Value

返されるコレクション内の項目のインデックスを示す整数です。

### 関連項目

* interface [IMediaList](../)
* package [com.aspose.html.dom.css](../../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../../)
