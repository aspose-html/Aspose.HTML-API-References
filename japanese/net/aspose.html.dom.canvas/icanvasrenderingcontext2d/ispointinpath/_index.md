---
title: ICanvasRenderingContext2D.IsPointInPath
second_title: Aspose.HTML for .NET API リファレンス
description: ICanvasRenderingContext2D 方法. 指定したポイントが現在のパスに含まれているかどうかを報告します
type: docs
weight: 260
url: /ja/net/aspose.html.dom.canvas/icanvasrenderingcontext2d/ispointinpath/
---
## IsPointInPath(double, double) {#ispointinpath_2}

指定したポイントが現在のパスに含まれているかどうかを報告します。

```csharp
public bool IsPointInPath(double x, double y)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| x | Double | チェックするポイントの X 座標。 |
| y | Double | チェックするポイントの Y 座標。 |

### 戻り値

ポイントがパスの塗りつぶしによって含まれる領域内にある場合は true、そうでない場合は false を返します。

### 関連項目

* interface [ICanvasRenderingContext2D](../)
* 名前空間 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 組み立て [Aspose.HTML](../../../)

---

## IsPointInPath(double, double, CanvasFillRule) {#ispointinpath_3}

指定したポイントが現在のパスに含まれているかどうかを報告します。

```csharp
public bool IsPointInPath(double x, double y, CanvasFillRule fillRule)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| x | Double | チェックするポイントの X 座標。 |
| y | Double | チェックするポイントの Y 座標。 |
| fillRule | CanvasFillRule | ポイントがパスの内側にあるかパスの外側にあるかを判断するためのアルゴリズム。 |

### 戻り値

ポイントがパスの塗りつぶしによって含まれる領域内にある場合は true、そうでない場合は false を返します。

### 関連項目

* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* 名前空間 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 組み立て [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double) {#ispointinpath}

指定したポイントが現在のパスに含まれているかどうかを報告します。

```csharp
public bool IsPointInPath(Path2D path, double x, double y)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| path | Path2D | チェックする Path2D パス。 |
| x | Double | チェックするポイントの X 座標。 |
| y | Double | チェックするポイントの Y 座標。 |

### 戻り値

ポイントがパスの塗りつぶしによって含まれる領域内にある場合は true、そうでない場合は false を返します。

### 関連項目

* class [Path2D](../../path2d/)
* interface [ICanvasRenderingContext2D](../)
* 名前空間 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 組み立て [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double, CanvasFillRule) {#ispointinpath_1}

指定したポイントが現在のパスに含まれているかどうかを報告します。

```csharp
public bool IsPointInPath(Path2D path, double x, double y, CanvasFillRule fillRule)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| path | Path2D | チェックする Path2D パス。 |
| x | Double | チェックするポイントの X 座標。 |
| y | Double | チェックするポイントの Y 座標。 |
| fillRule | CanvasFillRule | ポイントがパスの内側にあるかパスの外側にあるかを判断するためのアルゴリズム。 |

### 戻り値

ポイントがパスの塗りつぶしによって含まれる領域内にある場合は true、そうでない場合は false を返します。

### 関連項目

* class [Path2D](../../path2d/)
* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* 名前空間 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 組み立て [Aspose.HTML](../../../)


