---
title: "ICanvasRenderingContext2D.CreateImageData"
second_title: "Referensi API Aspose.HTML untuk Java"
description: "Metode ICanvasRenderingContext2D. Membuat objek ImageData kosong baru dengan dimensi yang ditentukan. Semua piksel dalam objek baru berwarna hitam transparan"
type: docs

url: /id/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/createimagedata/
---
## CreateImageData(double, double) {#createimagedata_1}

Membuat objek ImageData baru yang kosong dengan dimensi yang ditentukan. Semua piksel dalam objek baru tersebut berwarna hitam transparan.

```java
public IImageData CreateImageData(double sw, double sh)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| sw | Double | Lebar yang diberikan kepada objek ImageData baru. |
| sh | Double | Tinggi yang diberikan kepada objek ImageData baru. |

### Nilai Kembali

Objek ImageData baru dengan lebar dan tinggi yang ditentukan. Objek baru ini diisi dengan piksel hitam transparan.

### Lihat Juga

* interface [IImageData](../../iimagedata/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## CreateImageData(IImageData) {#createimagedata}

Membuat objek ImageData baru yang kosong dengan dimensi yang ditentukan. Semua piksel dalam objek baru tersebut berwarna hitam transparan.

```java
public IImageData CreateImageData(IImageData imagedata)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| imagedata | IImageData | Objek ImageData yang sudah ada yang akan disalin lebar dan tingginya. Gambar itu sendiri tidak disalin. |

### Nilai Kembali

Objek ImageData baru dengan lebar dan tinggi yang ditentukan. Objek baru ini diisi dengan piksel hitam transparan.

### Lihat Juga

* interface [IImageData](../../iimagedata/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
