---
title: "ICanvasRenderingContext2D.IsPointInPath"
second_title: "Referensi API Aspose.HTML untuk Java"
description: "Metode ICanvasRenderingContext2D. Melaporkan apakah titik yang ditentukan berada dalam jalur saat ini atau tidak"
type: docs

url: /id/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/ispointinpath/
---
## IsPointInPath(double, double) {#ispointinpath_2}

Melaporkan apakah titik yang ditentukan berada di dalam jalur saat ini atau tidak.

```java
public bool IsPointInPath(double x, double y)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| x | Double | Koordinat X titik yang akan diperiksa. |
| y | Double | Koordinat Y dari titik yang akan diperiksa. |

### Nilai Kembali

Mengembalikan true jika titik berada di dalam area yang diisi oleh jalur, jika tidak false.

### Lihat Juga

* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(double, double, CanvasFillRule) {#ispointinpath_3}

Melaporkan apakah titik yang ditentukan berada di dalam jalur saat ini atau tidak.

```java
public bool IsPointInPath(double x, double y, CanvasFillRule fillRule)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| x | Double | Koordinat X titik yang akan diperiksa. |
| y | Double | Koordinat Y dari titik yang akan diperiksa. |
| fillRule | CanvasFillRule | Algoritma yang digunakan untuk menentukan apakah titik berada di dalam jalur atau di luar jalur. |

### Nilai Kembali

Mengembalikan true jika titik berada di dalam area yang diisi oleh jalur, jika tidak false.

### Lihat Juga

* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double) {#ispointinpath}

Melaporkan apakah titik yang ditentukan berada di dalam jalur saat ini atau tidak.

```java
public bool IsPointInPath(Path2D path, double x, double y)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| jalur | Path2D | Sebuah jalur Path2D untuk diperiksa. |
| x | Double | Koordinat X titik yang akan diperiksa. |
| y | Double | Koordinat Y dari titik yang akan diperiksa. |

### Nilai Kembali

Mengembalikan true jika titik berada di dalam area yang diisi oleh jalur, jika tidak false.

### Lihat Juga

* class [Path2D](../../path2d/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double, CanvasFillRule) {#ispointinpath_1}

Melaporkan apakah titik yang ditentukan berada di dalam jalur saat ini atau tidak.

```java
public bool IsPointInPath(Path2D path, double x, double y, CanvasFillRule fillRule)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| jalur | Path2D | Sebuah jalur Path2D untuk diperiksa. |
| x | Double | Koordinat X titik yang akan diperiksa. |
| y | Double | Koordinat Y dari titik yang akan diperiksa. |
| fillRule | CanvasFillRule | Algoritma yang digunakan untuk menentukan apakah titik berada di dalam jalur atau di luar jalur. |

### Nilai Kembali

Mengembalikan true jika titik berada di dalam area yang diisi oleh jalur, jika tidak false.

### Lihat Juga

* class [Path2D](../../path2d/)
* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
