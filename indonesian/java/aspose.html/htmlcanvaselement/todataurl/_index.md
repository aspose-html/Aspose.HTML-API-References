---
title: "HTMLCanvasElement.ToDataURL"
second_title: "Referensi API Aspose.HTML untuk Java"
description: "HTMLCanvasElement method. mengembalikan data URI yang berisi representasi gambar dalam format yang ditentukan oleh parameter type, default PNG. Gambar yang dikembalikan memiliki resolusi 96 dpi"
type: docs

url: /id/java/com.aspose.html/htmlcanvaselement/todataurl/
---
## ToDataURL() {#todataurl}

mengembalikan URI data yang berisi representasi gambar dalam format yang ditentukan oleh parameter tipe (default ke PNG). Gambar yang dikembalikan memiliki resolusi 96 dpi.

```java
public String ToDataURL()
```

### Nilai Kembali

Sebuah DOMString yang berisi data URI yang diminta.

### Lihat Juga

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String) {#todataurl_1}

mengembalikan URI data yang berisi representasi gambar dalam format yang ditentukan oleh parameter tipe (default ke PNG). Gambar yang dikembalikan memiliki resolusi 96 dpi.

```java
public String ToDataURL(String type)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| tipe | String | Sebuah String yang menunjukkan format gambar. Tipe format default adalah image/png. |

### Nilai Kembali

Sebuah DOMString yang berisi data URI yang diminta.

### Lihat Juga

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String, double) {#todataurl_2}

mengembalikan URI data yang berisi representasi gambar dalam format yang ditentukan oleh parameter tipe (default ke PNG). Gambar yang dikembalikan memiliki resolusi 96 dpi.

```java
public String ToDataURL(String type, double encoderOptions)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| tipe | String | Sebuah String yang menunjukkan format gambar. Tipe format default adalah image/png. |
| encoderOptions | Double | Sebuah Number antara 0 dan 1 yang menunjukkan kualitas gambar jika tipe yang diminta adalah image/jpeg atau image/webp. |

### Nilai Kembali

Sebuah DOMString yang berisi data URI yang diminta.

### Lihat Juga

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)
