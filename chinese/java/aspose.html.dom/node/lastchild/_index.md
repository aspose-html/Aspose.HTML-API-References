---
title: "Node.LastChild"
second_title: "Aspose.HTML for Java API 参考"
description: "Node 属性。只读的 lastChild 属性（Node 接口）返回节点的最后一个子节点。如果其父节点是元素，则子节点通常是元素节点、文本节点或注释节点。如果没有子元素，则返回 null。"
type: docs

url: /zh/java/com.aspose.html.dom/node/lastchild/
---
## Node.LastChild property

只读的 lastChild 属性（[`Node`](../) 接口）返回节点的最后一个子节点。如果其父节点是元素，则子节点通常是元素节点、文本节点或注释节点。如果没有子元素，则返回 null。

```java
public Node LastChild { get; }
```

### Property Value

一个 [`Node`](../) ，它是节点的最后一个子节点；如果没有子元素，则为 null。

## 备注

参考：

[DOM Standard](https://dom.spec.whatwg.org/) - defines a platform-neutral model for events, aborting activities, and node trees.[DOM Standard (DOM) # dom-node-lastchild](https://dom.spec.whatwg.org/#dom-node-lastchild).[GitHub](https://github.com/whatwg/dom) - repository hosts the DOM Standard.

### 另请参阅

* class [Node](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
