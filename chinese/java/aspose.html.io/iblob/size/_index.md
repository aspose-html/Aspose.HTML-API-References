---
title: "IBlob.Size"
second_title: "Aspose.HTML for Java API 参考"
description: "IBlob 属性。返回字节序列的大小（以字节数计）。在获取时，符合规范的用户代理必须返回可以被 FileReader 或 FileReaderSync 对象读取的总字节数，如果 Blob 没有可读取的字节，则返回 0。"
type: docs

url: /zh/java/com.aspose.html.io/iblob/size/
---
## IBlob.Size property

返回字节序列的大小（以字节数计）。在获取时，符合规范的用户代理必须返回可以被 FileReader 或 FileReaderSync 对象读取的总字节数，如果 Blob 没有可读取的字节，则返回 0。

```java
public ulong Size { get; }
```

### 另请参阅

* interface [IBlob](../)
* package [com.aspose.html.io](../../../com.aspose.html.io/)
* package [Aspose.HTML](../../../)
