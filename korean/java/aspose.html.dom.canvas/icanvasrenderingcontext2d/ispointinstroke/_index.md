---
title: "ICanvasRenderingContext2D.IsPointInStroke"
second_title: "Java용 Aspose.HTML API 참조"
description: "ICanvasRenderingContext2D 메서드. 지정된 점이 경로의 스트로크 영역 내부에 있는지 여부를 보고합니다."
type: docs

url: /ko/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/ispointinstroke/
---
## IsPointInStroke(double, double) {#ispointinstroke_1}

지정된 점이 경로 스트로크에 의해 포함된 영역 내부에 있는지 여부를 보고합니다.

```java
public bool IsPointInStroke(double x, double y)
```

| Parameter | Type | 설명 |
| --- | --- | --- |
| x | Double | 확인할 점의 X 좌표. |
| y | Double | 검사할 점의 Y 좌표. |

### 반환 값

점이 경로의 스트로크 영역 안에 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInStroke(Path2D, double, double) {#ispointinstroke}

지정된 점이 경로 스트로크에 의해 포함된 영역 내부에 있는지 여부를 보고합니다.

```java
public bool IsPointInStroke(Path2D path, double x, double y)
```

| Parameter | Type | 설명 |
| --- | --- | --- |
| 경로 | Path2D | 검사할 Path2D 경로. |
| x | Double | 확인할 점의 X 좌표. |
| y | Double | 검사할 점의 Y 좌표. |

### 반환 값

점이 경로의 스트로크 영역 안에 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* class [Path2D](../../path2d/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
