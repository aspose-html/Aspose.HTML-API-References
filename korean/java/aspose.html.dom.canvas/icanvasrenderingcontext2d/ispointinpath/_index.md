---
title: "ICanvasRenderingContext2D.IsPointInPath"
second_title: "Aspose.HTML for Java API 참조"
description: "ICanvasRenderingContext2D 메서드. 지정된 점이 현재 경로에 포함되는지 여부를 보고합니다."
type: docs

url: /ko/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/ispointinpath/
---
## IsPointInPath(double, double) {#ispointinpath_2}

지정된 점이 현재 경로에 포함되는지 여부를 보고합니다.

```java
public bool IsPointInPath(double x, double y)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Double | 확인할 점의 X 좌표. |
| y | Double | 확인할 점의 Y 좌표. |

### 반환 값

점이 경로의 채우기 영역 안에 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(double, double, CanvasFillRule) {#ispointinpath_3}

지정된 점이 현재 경로에 포함되는지 여부를 보고합니다.

```java
public bool IsPointInPath(double x, double y, CanvasFillRule fillRule)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Double | 확인할 점의 X 좌표. |
| y | Double | 확인할 점의 Y 좌표. |
| fillRule | CanvasFillRule | 점이 경로 내부에 있는지 외부에 있는지를 판단하는 알고리즘. |

### 반환 값

점이 경로의 채우기 영역 안에 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double) {#ispointinpath}

지정된 점이 현재 경로에 포함되는지 여부를 보고합니다.

```java
public bool IsPointInPath(Path2D path, double x, double y)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 경로 | Path2D | 확인할 Path2D 경로. |
| x | Double | 확인할 점의 X 좌표. |
| y | Double | 확인할 점의 Y 좌표. |

### 반환 값

점이 경로의 채우기 영역 안에 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* class [Path2D](../../path2d/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double, CanvasFillRule) {#ispointinpath_1}

지정된 점이 현재 경로에 포함되는지 여부를 보고합니다.

```java
public bool IsPointInPath(Path2D path, double x, double y, CanvasFillRule fillRule)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 경로 | Path2D | 확인할 Path2D 경로. |
| x | Double | 확인할 점의 X 좌표. |
| y | Double | 확인할 점의 Y 좌표. |
| fillRule | CanvasFillRule | 점이 경로 내부에 있는지 외부에 있는지를 판단하는 알고리즘. |

### 반환 값

점이 경로의 채우기 영역 안에 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* class [Path2D](../../path2d/)
* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
