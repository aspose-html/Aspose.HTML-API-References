---
title: "ICanvasRenderingContext2D.CreateImageData"
second_title: "Java용 Aspose.HTML API 참조"
description: "ICanvasRenderingContext2D 메서드. 지정된 크기로 새로운 빈 ImageData 객체를 생성합니다. 새 객체의 모든 픽셀은 투명한 검정색입니다."
type: docs

url: /ko/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/createimagedata/
---
## CreateImageData(double, double) {#createimagedata_1}

지정된 차원으로 새로운 빈 ImageData 객체를 생성합니다. 새 객체의 모든 픽셀은 투명 검은색입니다.

```java
public IImageData CreateImageData(double sw, double sh)
```

| Parameter | Type | 설명 |
| --- | --- | --- |
| sw | Double | 새 ImageData 객체에 부여할 너비. |
| sh | Double | 새 ImageData 객체에 부여할 높이. |

### 반환 값

지정된 너비와 높이를 가진 새로운 ImageData 객체입니다. 새로운 객체는 투명한 검은색 픽셀로 채워집니다.

### 또 보기

* interface [IImageData](../../iimagedata/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## CreateImageData(IImageData) {#createimagedata}

지정된 차원으로 새로운 빈 ImageData 객체를 생성합니다. 새 객체의 모든 픽셀은 투명 검은색입니다.

```java
public IImageData CreateImageData(IImageData imagedata)
```

| Parameter | Type | 설명 |
| --- | --- | --- |
| 이미지데이터 | IImageData | 너비와 높이를 복사할 기존 ImageData 객체입니다. 이미지 자체는 복사되지 않습니다. |

### 반환 값

지정된 너비와 높이를 가진 새로운 ImageData 객체입니다. 새로운 객체는 투명한 검은색 픽셀로 채워집니다.

### 또 보기

* interface [IImageData](../../iimagedata/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
