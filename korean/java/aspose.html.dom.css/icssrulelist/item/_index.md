---
title: "ICSSRuleList.Item"
second_title: "Aspose.HTML for Java API 참조"
description: "ICSSRuleList 속성. 메서드 item(http://www.w3.org/TR/DOM-Level-2-Style/css.htmlCSS-CSSRuleList)을 사용하여 CSS 규칙을 검색합니다. 이 컬렉션의 순서는 CSS 스타일 시트의 규칙 순서를 나타냅니다. 인덱스가 리스트의 규칙 수보다 크거나 같으면 null을 반환합니다"
type: docs

url: /ko/java/com.aspose.html.dom.css/icssrulelist/item/
---
## ICSSRuleList indexer

메서드 item() (http://www.w3.org/TR/DOM-Level-2-Style/css.html#CSS-CSSRuleList)을 사용하여 CSS 규칙을 검색합니다. 이 컬렉션의 순서는 CSS 스타일 시트의 규칙 순서를 나타냅니다. 인덱스가 리스트의 규칙 수보다 크거나 같으면 null을 반환합니다.

```java
public ICSSRule this[int index] { get; }
```

### 반환 값

item(index) 메서드는 컬렉션에서 index번째 CSSRule 객체를 반환해야 합니다. 컬렉션에 index번째 객체가 없으면 메서드는 null을 반환해야 합니다.

### Property Value

CSS 규칙 컬렉션의 인덱스.

### 또 보기

* interface [ICSSRule](../../icssrule/)
* interface [ICSSRuleList](../)
* package [com.aspose.html.dom.css](../../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../../)
