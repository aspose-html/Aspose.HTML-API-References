---
title: "XPathResultType 열거형"
second_title: "Java용 Aspose.HTML API 참조"
description: "com.aspose.html.dom.xpath.XPathResultType 열거형. 이 결과가 어떤 유형인지 나타내는 부호 없는 짧은 정수입니다. 특정 유형이 지정되면 필요한 경우 XPath 유형 변환을 사용하여 해당 유형으로 결과가 반환됩니다."
type: docs

url: /ko/java/com.aspose.html.dom.xpath/xpathresulttype/
---
## XPathResultType enumeration

이 결과가 어떤 유형인지 나타내는 부호 없는 짧은 정수입니다. 특정 `type`이 지정되면, 필요한 경우 및 가능한 경우 XPath 유형 변환을 사용하여 해당 유형으로 결과가 반환됩니다.

```java
public enum XPathResultType
```

### 값

| 이름 | 값 | 설명 |
| --- | --- | --- |
| Any | `0` | 이 코드는 특정 유형을 나타내지 않습니다. XPath 식의 평가에서는 이 유형이 절대 생성되지 않습니다. 이 유형이 요청되면 평가 결과는 식 평가에서 자연스럽게 도출되는 유형을 반환합니다. `Any` 유형이 요청되었을 때 자연 결과가 노드 집합인 경우, `UnorderedNodeIterator`는 항상 결과 유형이 됩니다. 노드 집합의 다른 표현은 명시적으로 요청해야 합니다. |
| Number | `1` | 결과는 [XPath 1.0]에 정의된 숫자입니다. 문서가 수정되어도 숫자는 무효화되지 않지만, 재평가 시 동일한 숫자를 얻지 못할 수 있습니다. |
| String | `2` | 결과는 [XPath 1.0]에 정의된 문자열입니다. 문서가 수정되어도 문자열은 무효화되지 않지만, 현재 문서와 더 이상 일치하지 않을 수 있습니다. |
| Boolean | `3` | 결과는 [XPath 1.0]에 정의된 부울값입니다. 문서가 수정되어도 부울값은 무효화되지 않지만, 재평가 시 동일한 부울값을 얻지 못할 수 있습니다. |
| UnorderedNodeIterator | `4` | 결과는 [XPath 1.0]에 정의된 노드 집합이며, 반복적으로 접근됩니다. 특정 순서대로 노드가 제공되지 않을 수 있습니다. 문서가 수정되면 반복이 무효화됩니다. 결과가 노드 집합이고 `Any`type이 요청된 경우 반환되는 기본 유형입니다. |
| OrderedNodeIterator | `5` | 결과는 [XPath 1.0]에 정의된 노드 집합이며, 반복적으로 접근되어 문서 순서대로 노드를 생성합니다. 문서가 수정되면 반복이 무효화됩니다. |
| UnorderedNodeSnapshot | `6` | 결과는 [XPath 1.0]에 정의된 노드 집합이며, 특정 순서가 아닐 수 있는 노드들의 스냅샷 리스트로 접근됩니다. 문서가 수정되어도 스냅샷은 무효화되지 않지만, 재평가 시 동일한 스냅샷을 얻지 못할 수 있으며 스냅샷 내 노드가 변경, 이동 또는 문서에서 제거될 수 있습니다. |
| OrderedNodeSnapshot | `7` | 결과는 [XPath 1.0]에 정의된 노드 집합이며, 원본 문서 순서대로 정렬된 노드들의 스냅샷 리스트로 접근됩니다. 문서가 수정되어도 스냅샷은 무효화되지 않지만, 재평가 시 동일한 스냅샷을 얻지 못할 수 있으며 스냅샷 내 노드가 변경, 이동 또는 문서에서 제거될 수 있습니다. |
| AnyUnorderedNode | `8` | 결과는 [XPath 1.0]에 정의된 노드 집합이며 단일 노드로 접근됩니다. 노드 집합이 비어 있으면 `null`일 수 있습니다. 문서가 수정되어도 노드는 무효화되지 않지만, 결과 노드가 현재 문서와 더 이상 일치하지 않을 수 있습니다. 이는 구현이 결과 집합에서 하나의 노드만 찾으면 중단할 수 있어 최적화를 가능하게 하는 편의 기능입니다. 실제 결과에 여러 노드가 있는 경우 반환된 단일 노드가 문서 순서상의 첫 번째 노드가 아닐 수 있습니다. |
| FirstOrderedNode | `9` | 결과는 [XPath 1.0]에 정의된 노드 집합이며 단일 노드로 접근됩니다. 노드 집합이 비어 있으면 `null`일 수 있습니다. 문서가 수정되어도 노드는 무효화되지 않지만, 결과 노드가 현재 문서와 더 이상 일치하지 않을 수 있습니다. 이는 구현이 결과 집합에서 문서 순서상의 첫 번째 노드를 찾으면 중단할 수 있어 최적화를 가능하게 하는 편의 기능입니다. 실제 결과에 여러 노드가 있는 경우 반환된 단일 노드는 문서 순서상의 첫 번째 노드가 됩니다. |

### 또 보기

* package [com.aspose.html.dom.xpath](../../com.aspose.html.dom.xpath/)
* package [Aspose.HTML](../../)
