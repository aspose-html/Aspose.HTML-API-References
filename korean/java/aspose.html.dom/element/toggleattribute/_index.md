---
title: "Element.ToggleAttribute"
second_title: "Aspose.HTML for Java API 참조"
description: "Element 메서드. force가 제공되지 않은 경우 qualifiedName을 토글하여, 존재하면 제거하고 존재하지 않으면 추가합니다. force가 true이면 qualifiedName을 추가하고, false이면 qualifiedName을 제거합니다."
type: docs

url: /ko/java/com.aspose.html.dom/element/toggleattribute/
---
## ToggleAttribute(String) {#toggleattribute}

force가 제공되지 않으면 qualifiedName을 "토글"하여 존재하면 제거하고 존재하지 않으면 추가합니다. force가 true이면 qualifiedName을 추가하고, false이면 qualifiedName을 제거합니다.

```java
public bool ToggleAttribute(String qualifiedName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| qualifiedName | String | 속성 QualifiedName. |

### 반환 값

qualifiedName이 현재 존재하면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* class [Element](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)

---

## ToggleAttribute(String, bool) {#toggleattribute_1}

force가 제공되지 않으면 qualifiedName을 "토글"하여 존재하면 제거하고 존재하지 않으면 추가합니다. force가 true이면 qualifiedName을 추가하고, false이면 qualifiedName을 제거합니다.

```java
public bool ToggleAttribute(String qualifiedName, bool force)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| qualifiedName | String | 속성 QualifiedName. |
| force | Boolean | 속성을 토글하기 위한 force 옵션. |

### 반환 값

qualifiedName이 현재 존재하면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* class [Element](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
