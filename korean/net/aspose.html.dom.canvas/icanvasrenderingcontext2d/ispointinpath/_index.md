---
title: ICanvasRenderingContext2D.IsPointInPath
second_title: .NET API 참조용 Aspose.HTML
description: ICanvasRenderingContext2D 방법. 지정된 지점이 현재 경로에 포함되어 있는지 여부를 보고합니다.
type: docs
weight: 260
url: /ko/net/aspose.html.dom.canvas/icanvasrenderingcontext2d/ispointinpath/
---
## IsPointInPath(double, double) {#ispointinpath_2}

지정된 지점이 현재 경로에 포함되어 있는지 여부를 보고합니다.

```csharp
public bool IsPointInPath(double x, double y)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| x | Double | 확인할 점의 X좌표. |
| y | Double | 확인할 점의 Y좌표. |

### 반환 값

점이 경로 채우기에 포함된 영역 안에 있으면 true를 반환하고 그렇지 않으면 false를 반환합니다.

### 또한보십시오

* interface [ICanvasRenderingContext2D](../)
* 네임스페이스 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 집회 [Aspose.HTML](../../../)

---

## IsPointInPath(double, double, CanvasFillRule) {#ispointinpath_3}

지정된 지점이 현재 경로에 포함되어 있는지 여부를 보고합니다.

```csharp
public bool IsPointInPath(double x, double y, CanvasFillRule fillRule)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| x | Double | 확인할 점의 X좌표. |
| y | Double | 확인할 점의 Y좌표. |
| fillRule | CanvasFillRule | 점이 경로 내부에 있는지 경로 외부에 있는지 결정하는 알고리즘입니다. |

### 반환 값

점이 경로 채우기에 포함된 영역 안에 있으면 true를 반환하고 그렇지 않으면 false를 반환합니다.

### 또한보십시오

* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* 네임스페이스 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 집회 [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double) {#ispointinpath}

지정된 지점이 현재 경로에 포함되어 있는지 여부를 보고합니다.

```csharp
public bool IsPointInPath(Path2D path, double x, double y)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| path | Path2D | 확인할 Path2D 경로입니다. |
| x | Double | 확인할 점의 X좌표. |
| y | Double | 확인할 점의 Y좌표. |

### 반환 값

점이 경로 채우기에 포함된 영역 안에 있으면 true를 반환하고 그렇지 않으면 false를 반환합니다.

### 또한보십시오

* class [Path2D](../../path2d/)
* interface [ICanvasRenderingContext2D](../)
* 네임스페이스 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 집회 [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double, CanvasFillRule) {#ispointinpath_1}

지정된 지점이 현재 경로에 포함되어 있는지 여부를 보고합니다.

```csharp
public bool IsPointInPath(Path2D path, double x, double y, CanvasFillRule fillRule)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| path | Path2D | 확인할 Path2D 경로입니다. |
| x | Double | 확인할 점의 X좌표. |
| y | Double | 확인할 점의 Y좌표. |
| fillRule | CanvasFillRule | 점이 경로 내부에 있는지 경로 외부에 있는지 결정하는 알고리즘입니다. |

### 반환 값

점이 경로 채우기에 포함된 영역 안에 있으면 true를 반환하고 그렇지 않으면 false를 반환합니다.

### 또한보십시오

* class [Path2D](../../path2d/)
* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* 네임스페이스 [Aspose.Html.Dom.Canvas](../../icanvasrenderingcontext2d/)
* 집회 [Aspose.HTML](../../../)


