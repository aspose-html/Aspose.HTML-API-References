---
title: "ICanvasRenderingContext2D.FillText"
second_title: "Aspose.HTML für Java API-Referenz"
description: "ICanvasRenderingContext2D-Methode. Zeichnet und füllt einen angegebenen Text an der angegebenen xy‑Position."
type: docs

url: /de/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/filltext/
---
## FillText(String, double, double) {#filltext}

Zeichnet (füllt) einen angegebenen Text an der angegebenen Position (x,y).

```java
public void FillText(String text, double x, double y)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| text | String | Der zu zeichnende Text unter Verwendung der aktuellen Schriftart, textAlign, textBaseline und direction‑Werte. |
| x | Double | Die x‑Achse der Koordinate für den Startpunkt des Textes. |
| y | Double | Die y‑Achse der Koordinate für den Startpunkt des Textes. |

### Siehe auch

* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## FillText(String, double, double, double) {#filltext_1}

Zeichnet (füllt) einen angegebenen Text an der angegebenen Position (x,y).

```java
public void FillText(String text, double x, double y, double maxWidth)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| text | String | Der zu zeichnende Text unter Verwendung der aktuellen Schriftart, textAlign, textBaseline und direction‑Werte. |
| x | Double | Die x‑Achse der Koordinate für den Startpunkt des Textes. |
| y | Double | Die y‑Achse der Koordinate für den Startpunkt des Textes. |
| maxWidth | Double | Die maximale Breite, die gezeichnet werden soll. Wenn angegeben und die Zeichenkette breiter als diese Breite berechnet wird, wird die Schriftart angepasst, um eine stärker horizontal komprimierte Schrift zu verwenden (falls verfügbar oder falls eine einigermaßen lesbare Schrift durch horizontales Skalieren der aktuellen Schrift synthetisiert werden kann) oder eine kleinere Schrift. |

### Siehe auch

* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
