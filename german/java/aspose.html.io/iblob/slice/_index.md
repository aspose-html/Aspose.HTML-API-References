---
title: "IBlob.Slice"
second_title: "Aspose.HTML für Java API-Referenz"
description: "IBlob-Methode. Gibt ein neues Blob‑Objekt zurück, dessen Bytes vom optionalen Start‑Parameter bis (aber ohne) den optionalen End‑Parameter reichen und das ein type‑Attribut besitzt, das den Wert des optionalen contentType‑Parameters hat."
type: docs

url: /de/java/com.aspose.html.io/iblob/slice/
---
## IBlob.Slice method

Gibt ein neues Blob-Objekt zurück, dessen Bytes vom optionalen start‑Parameter bis (aber ohne) den optionalen end‑Parameter reichen, und dessen type‑Attribut den Wert des optionalen contentType‑Parameters hat.

```java
public IBlob Slice(ulong start, ulong end, String contentType)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Start | UInt64 | Der Parameter ist ein Wert für den Startpunkt eines Slice. |
| Ende | UInt64 | Der Parameter ist ein Wert für den Startpunkt eines Slice. |
| contentType | String | Der Parameter ist ein Medientyp des Blob. |

### Rückgabewert

Gibt ein neues Blob-Objekt zurück, dessen Bytes vom optionalen start‑Parameter bis (aber ohne) den optionalen end‑Parameter reichen, und dessen type‑Attribut den Wert des optionalen contentType‑Parameters hat.

### Siehe auch

* interface [IBlob](../)
* package [com.aspose.html.io](../../../com.aspose.html.io/)
* package [Aspose.HTML](../../../)
