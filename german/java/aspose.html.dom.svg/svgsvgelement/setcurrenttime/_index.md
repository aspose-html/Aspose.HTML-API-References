---
title: "SVGSVGElement.SetCurrentTime"
second_title: "Aspose.HTML für Java API-Referenz"
description: "SVGSVGElement‑Methode. Passt die Uhr für dieses SVG‑Dokumentfragment an und legt eine neue aktuelle Zeit fest. Wenn setCurrentTime aufgerufen wird, bevor die Dokument‑Zeitlinie begonnen hat, zum Beispiel durch ein Skript, das in einem Skript‑Element ausgeführt wird, bevor das SVGLoad‑Ereignis des Dokuments gesendet wird, dann gibt der Sekundenwert beim letzten Aufruf der Methode die Zeit an, zu der das Dokument springen wird, sobald die Dokument‑Zeitlinie begonnen hat."
type: docs

url: /de/java/com.aspose.html.dom.svg/svgsvgelement/setcurrenttime/
---
## SVGSVGElement.SetCurrentTime method

Passt die Uhr für dieses SVG-Dokumentfragment an und legt eine neue aktuelle Zeit fest. Wenn setCurrentTime aufgerufen wird, bevor die Dokumentzeitlinie begonnen hat (zum Beispiel durch ein Skript, das in einem ‘script’-Element läuft, bevor das SVGLoad-Ereignis des Dokuments gesendet wird), dann gibt der Sekundenwert beim letzten Aufruf der Methode die Zeit an, zu der das Dokument springen wird, sobald die Dokumentzeitlinie begonnen hat.

```java
public void SetCurrentTime(float seconds)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Sekunden | Single | Die neue aktuelle Zeit in Sekunden relativ zum Startzeitpunkt des aktuellen SVG‑Dokumentfragments. |

### Siehe auch

* class [SVGSVGElement](../)
* package [com.aspose.html.dom.svg](../../../com.aspose.html.dom.svg/)
* package [Aspose.HTML](../../../)
