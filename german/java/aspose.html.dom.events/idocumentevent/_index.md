---
title: "IDocumentEvent‑Schnittstelle"
second_title: "Aspose.HTML für Java API-Referenz"
description: "com.aspose.html.dom.events.IDocumentEvent‑Schnittstelle. Die DocumentEvent‑Schnittstelle bietet einen Mechanismus, mit dem der Benutzer ein Ereignis eines von der Implementierung unterstützten Typs erstellen kann. Es wird erwartet, dass die DocumentEvent‑Schnittstelle auf demselben Objekt implementiert wird, das die Document‑Schnittstelle in einer Implementierung, die das Ereignismodell unterstützt, implementiert."
type: docs

url: /de/java/com.aspose.html.dom.events/idocumentevent/
---
## IDocumentEvent interface

Das DocumentEvent-Interface stellt einen Mechanismus bereit, mit dem der Benutzer ein Ereignis eines von der Implementierung unterstützten Typs erstellen kann. Es wird erwartet, dass das DocumentEvent-Interface auf demselben Objekt implementiert wird, das das Document-Interface in einer Implementierung, die das Ereignismodell unterstützt, implementiert.

```java
public interface IDocumentEvent
```

## Methoden

| Name | Beschreibung |
| --- | --- |
| [createEvent](../../com.aspose.html.dom.events/idocumentevent/createevent/)(String) | Die Methode createEvent wird zur Erstellung von Ereignissen verwendet, wenn es für den Benutzer unpraktisch oder unnötig ist, ein Ereignis selbst zu erstellen. |

## Hinweise

Es wird erwartet, dass die Schnittstelle auf demselben Objekt implementiert wird, das die Schnittstelle in einer Implementierung, die das Ereignismodell unterstützt, implementiert.

### Siehe auch

* package [com.aspose.html.dom.events](../../com.aspose.html.dom.events/)
* package [Aspose.HTML](../../)
