---
title: "Node.ReplaceChild"
second_title: "Aspose.HTML für Java API-Referenz"
description: "Node-Methode. Ersetzt den Kindknoten oldChild durch newChild in der Kindliste und gibt den alten oldChild‑Knoten zurück. Wenn newChild ein DocumentFragment‑Objekt ist, wird oldChild durch alle Kinder des DocumentFragment ersetzt, die in derselben Reihenfolge eingefügt werden. Wenn newChild bereits im Baum ist, wird es zuerst entfernt."
type: docs

url: /de/java/com.aspose.html.dom/node/replacechild/
---
## Node.ReplaceChild method

Ersetzt den Kindknoten oldChild durch newChild in der Kindliste und gibt den alten oldChild‑Knoten zurück. Wenn newChild ein [`DocumentFragment`](../../documentfragment/)‑Objekt ist, wird oldChild durch alle [`DocumentFragment`](../../documentfragment/)‑Kinder ersetzt, die in derselben Reihenfolge eingefügt werden. Wenn newChild bereits im Baum ist, wird es zuerst entfernt.

```java
public Node ReplaceChild(Node node, Node child)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Knoten | Node | Der neue Knoten, der oldChild ersetzt. |
| Kind | Node | Das zu ersetzende Kind. |

### Rückgabewert

Der ersetzte Node. Dies ist derselbe Knoten wie oldChild.

### Siehe auch

* class [Node](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
