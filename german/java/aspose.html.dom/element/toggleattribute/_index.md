---
title: "Element.ToggleAttribute"
second_title: "Aspose.HTML für Java API-Referenz"
description: "Element-Methode. Wenn force nicht angegeben ist, schaltet qualifiedName um, indem es entfernt wird, falls es vorhanden ist, und hinzugefügt wird, falls es nicht vorhanden ist. Wenn force true ist, wird qualifiedName hinzugefügt. Wenn force false ist, wird qualifiedName entfernt."
type: docs

url: /de/java/com.aspose.html.dom/element/toggleattribute/
---
## ToggleAttribute(String) {#toggleattribute}

Wenn force nicht angegeben ist, wird qualifiedName „gewechselt“, d. h. es wird entfernt, wenn es vorhanden ist, und hinzugefügt, wenn es nicht vorhanden ist. Ist force true, wird qualifiedName hinzugefügt. Ist force false, wird qualifiedName entfernt.

```java
public bool ToggleAttribute(String qualifiedName)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| qualifiedName | String | Das Attribut QualifiedName. |

### Rückgabewert

Gibt true zurück, wenn qualifiedName jetzt vorhanden ist; andernfalls false.

### Siehe auch

* class [Element](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)

---

## ToggleAttribute(String, bool) {#toggleattribute_1}

Wenn force nicht angegeben ist, wird qualifiedName „gewechselt“, d. h. es wird entfernt, wenn es vorhanden ist, und hinzugefügt, wenn es nicht vorhanden ist. Ist force true, wird qualifiedName hinzugefügt. Ist force false, wird qualifiedName entfernt.

```java
public bool ToggleAttribute(String qualifiedName, bool force)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| qualifiedName | String | Das Attribut QualifiedName. |
| force | Boolean | Die force-Option zum Umschalten des Attributs. |

### Rückgabewert

Gibt true zurück, wenn qualifiedName jetzt vorhanden ist; andernfalls false.

### Siehe auch

* class [Element](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
