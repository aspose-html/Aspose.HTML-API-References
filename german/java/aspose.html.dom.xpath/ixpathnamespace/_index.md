---
title: "IXPathNamespace-Schnittstelle"
second_title: "Aspose.HTML für Java API-Referenz"
description: "com.aspose.html.dom.xpath.IXPathNamespace-Schnittstelle. Die XPathNamespace-Schnittstelle wird von XPathResult‑Schnittstellen zurückgegeben, um den XPath-Paketknoten‑Typ darzustellen, den das DOM nicht besitzt."
type: docs

url: /de/java/com.aspose.html.dom.xpath/ixpathpackage/
---
## IXPathNamespace interface

Das XPathNamespace Interface wird von XPathResult-Interfaces zurückgegeben, um den XPath-Paketknotentyp darzustellen, den das DOM nicht besitzt.

```java
public interface IXPathNamespace
```

## Eigenschaften

| Name | Beschreibung |
| --- | --- |
| [getOwnerElement](../../com.aspose.html.dom.xpath/ixpathpackage/ownerelement/) Das [`Element`](../../com.aspose.html.dom/element/) , auf dem das Paket im Geltungsbereich war, als es angefordert wurde. Dies ändert sich nicht bei einem zurückgegebenen Paketknoten, selbst wenn das Dokument geändert wird, sodass das Paket für dieses Element aus dem Geltungsbereich fällt und dieser Knoten dort nicht mehr durch XPath gefunden wird. |

### Siehe auch

* package [com.aspose.html.dom.xpath](../../com.aspose.html.dom.xpath/)
* package [Aspose.HTML](../../)
