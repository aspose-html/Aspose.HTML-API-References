---
title: "Attr.TextContent"
second_title: "Referencia de la API de Aspose.HTML para Java"
description: "Propiedad Attr. Este atributo devuelve el contenido de texto de este nodo y sus descendientes. Cuando está definido como null, asignarle un valor no tiene efecto. Al asignarlo, se eliminan todos los posibles hijos que pueda tener este nodo y, si la nueva String no está vacía o es null, se reemplaza por un único nodo Text que contiene la String; este atributo se establece con ella."
type: docs

url: /es/java/com.aspose.html.dom/attr/textcontent/
---
## Attr.TextContent property

Este atributo devuelve el contenido de texto de este nodo y sus descendientes. Cuando se define como null, establecerlo no tiene efecto. Al establecerlo, cualquier hijo posible que pueda tener este nodo se elimina y, si la nueva cadena no está vacía ni es null, se reemplaza por un único nodo Text que contiene la cadena a la que se establece este atributo.

```java
public String TextContent { get; set; }
```

### Property Value

El contenido del texto.

### Ver también

* class [Attr](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
