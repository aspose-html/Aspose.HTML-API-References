---
title: "HTMLCanvasElement.ToDataURL"
second_title: "Αναφορά API του Aspose.HTML για Java"
description: "HTMLCanvasElement method. επιστρέφει ένα data URI που περιέχει μια αναπαράσταση της εικόνας στη μορφή που καθορίζεται από την παράμετρο type, προεπιλογή PNG. Η επιστρεφόμενη εικόνα είναι σε ανάλυση 96 dpi"
type: docs

url: /el/java/com.aspose.html/htmlcanvaselement/todataurl/
---
## ToDataURL() {#todataurl}

επιστρέφει ένα URI δεδομένων που περιέχει μια αναπαράσταση της εικόνας στη μορφή που καθορίζεται από την παράμετρο τύπου (προεπιλογή PNG). Η επιστρεφόμενη εικόνα έχει ανάλυση 96 dpi.

```java
public String ToDataURL()
```

### Τιμή επιστροφής

Ένα DOMString που περιέχει το ζητούμενο data URI.

### Δείτε επίσης

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String) {#todataurl_1}

επιστρέφει ένα URI δεδομένων που περιέχει μια αναπαράσταση της εικόνας στη μορφή που καθορίζεται από την παράμετρο τύπου (προεπιλογή PNG). Η επιστρεφόμενη εικόνα έχει ανάλυση 96 dpi.

```java
public String ToDataURL(String type)
```

| Parameter | Type | Περιγραφή |
| --- | --- | --- |
| τύπος | String | Ένα String που υποδεικνύει τη μορφή της εικόνας. Ο προεπιλεγμένος τύπος μορφής είναι image/png. |

### Τιμή επιστροφής

Ένα DOMString που περιέχει το ζητούμενο data URI.

### Δείτε επίσης

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String, double) {#todataurl_2}

επιστρέφει ένα URI δεδομένων που περιέχει μια αναπαράσταση της εικόνας στη μορφή που καθορίζεται από την παράμετρο τύπου (προεπιλογή PNG). Η επιστρεφόμενη εικόνα έχει ανάλυση 96 dpi.

```java
public String ToDataURL(String type, double encoderOptions)
```

| Parameter | Type | Περιγραφή |
| --- | --- | --- |
| τύπος | String | Ένα String που υποδεικνύει τη μορφή της εικόνας. Ο προεπιλεγμένος τύπος μορφής είναι image/png. |
| encoderOptions | Double | Ένας Number μεταξύ 0 και 1 που υποδεικνύει την ποιότητα της εικόνας εάν ο ζητούμενος τύπος είναι image/jpeg ή image/webp. |

### Τιμή επιστροφής

Ένα DOMString που περιέχει το ζητούμενο data URI.

### Δείτε επίσης

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)
