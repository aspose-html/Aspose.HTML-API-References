---
title: "ICanvasRenderingContext2D.CreatePattern"
second_title: "Αναφορά API του Aspose.HTML για Java"
description: "Μέθοδος ICanvasRenderingContext2D. Δημιουργεί ένα μοτίβο χρησιμοποιώντας την καθορισμένη εικόνα ως CanvasImageSource. Επαναλαμβάνει την πηγή στις κατευθύνσεις που ορίζονται από το όρισμα repetition."
type: docs

url: /el/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/createpattern/
---
## CreatePattern(HTMLImageElement, String) {#createpattern_1}

Δημιουργεί μοτίβο χρησιμοποιώντας την καθορισμένη εικόνα (CanvasImageSource). Επαναλαμβάνει την πηγή στις κατευθύνσεις που ορίζονται από το όρισμα επανάληψης.

```java
public ICanvasPattern CreatePattern(HTMLImageElement image, String repetition)
```

| Parameter | Type | Περιγραφή |
| --- | --- | --- |
| εικόνα | HTMLImageElement | Ένα HTMLImageElement που θα χρησιμοποιηθεί ως η εικόνα προς επανάληψη. |
| repetition | String | Μια String που υποδεικνύει πώς να επαναληφθεί η εικόνα. |

### Τιμή επιστροφής

Ένα αδιαφανές αντικείμενο που περιγράφει ένα μοτίβο.

### Δείτε επίσης

* interface [ICanvasPattern](../../icanvaspattern/)
* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## CreatePattern(HTMLCanvasElement, String) {#createpattern}

Δημιουργεί μοτίβο χρησιμοποιώντας την καθορισμένη εικόνα (CanvasImageSource). Επαναλαμβάνει την πηγή στις κατευθύνσεις που ορίζονται από το όρισμα επανάληψης.

```java
public ICanvasPattern CreatePattern(HTMLCanvasElement image, String repetition)
```

| Parameter | Type | Περιγραφή |
| --- | --- | --- |
| εικόνα | HTMLCanvasElement | Ένα HTMLCanvasElement που θα χρησιμοποιηθεί ως η εικόνα προς επανάληψη. |
| repetition | String | Μια String που υποδεικνύει πώς να επαναληφθεί η εικόνα. |

### Τιμή επιστροφής

Ένα αδιαφανές αντικείμενο που περιγράφει ένα μοτίβο.

### Δείτε επίσης

* interface [ICanvasPattern](../../icanvaspattern/)
* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
