---
title: "Διεπαφή IDocumentEvent"
second_title: "Αναφορά API του Aspose.HTML για Java"
description: "Διεπαφή com.aspose.html.dom.events.IDocumentEvent. Η διεπαφή DocumentEvent παρέχει έναν μηχανισμό με τον οποίο ο χρήστης μπορεί να δημιουργήσει ένα συμβάν τύπου που υποστηρίζεται από την υλοποίηση. Αναμένεται ότι η διεπαφή DocumentEvent θα υλοποιηθεί στο ίδιο αντικείμενο που υλοποιεί τη διεπαφή Document σε μια υλοποίηση που υποστηρίζει το μοντέλο συμβάντων."
type: docs

url: /el/java/com.aspose.html.dom.events/idocumentevent/
---
## IDocumentEvent interface

Το interface DocumentEvent παρέχει έναν μηχανισμό με τον οποίο ο χρήστης μπορεί να δημιουργήσει ένα Event τύπου που υποστηρίζεται από την υλοποίηση. Αναμένεται ότι το interface DocumentEvent θα υλοποιηθεί στο ίδιο αντικείμενο που υλοποιεί το interface Document σε μια υλοποίηση που υποστηρίζει το μοντέλο Event.

```java
public interface IDocumentEvent
```

## Μέθοδοι

| Όνομα | Περιγραφή |
| --- | --- |
| [createEvent](../../com.aspose.html.dom.events/idocumentevent/createevent/)(String) | Η μέθοδος createEvent χρησιμοποιείται για τη δημιουργία συμβάντων όταν είναι είτε άβολη είτε περιττή η δημιουργία ενός συμβάντος από τον χρήστη. |

## Παρατηρήσεις

Αναμένεται ότι η διεπαφή θα υλοποιηθεί στο ίδιο αντικείμενο που υλοποιεί τη διεπαφή σε μια υλοποίηση που υποστηρίζει το μοντέλο συμβάντων.

### Δείτε επίσης

* package [com.aspose.html.dom.events](../../com.aspose.html.dom.events/)
* package [Aspose.HTML](../../)
