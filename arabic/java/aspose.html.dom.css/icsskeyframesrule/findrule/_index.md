---
title: "ICSSKeyframesRule.FindRule"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة ICSSKeyframesRule. طريقة findRule تُعيد القاعدة التي لها مفتاح يطابق المفتاح الممرَّر. إذا لم توجد مثل هذه القاعدة تُعاد قيمة null."
type: docs

url: /ar/java/com.aspose.html.dom.css/icsskeyframesrule/findrule/
---
## ICSSKeyframesRule.FindRule method

طريقة findRule تُعيد القاعدة التي لها مفتاح يطابق المفتاح الممرر. إذا لم توجد مثل هذه القاعدة، تُرجع قيمة null.

```java
public ICSSKeyframeRule FindRule(String key)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| كاي | String | المفتاح الذي يصف القاعدة المراد العثور عليها. يجب أن يكون المفتاح رقمًا بين 0 و 1، وإلا تُتجاهل القاعدة. |

### قيمة الإرجاع

تُعيد آخر [`CSSKeyframeRule`](../../icsskeyframerule/) مُعلن يطابق محدد الإطار المفتاح المحدد. إذا لم توجد قاعدة مطابقة، لا تفعل الطريقة شيئًا.

### انظر أيضًا

* interface [ICSSKeyframeRule](../../icsskeyframerule/)
* interface [ICSSKeyframesRule](../)
* package [com.aspose.html.dom.css](../../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../../)
