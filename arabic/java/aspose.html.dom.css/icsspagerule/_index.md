---
title: "واجهة ICSSPageRule"
second_title: "مرجع API لـ Aspose.HTML للـ Java"
description: "com.aspose.html.dom.css.ICSSPageRule interface. تمثل واجهة CSSPageRule قاعدة صفحة داخل ورقة أنماط CSS. تُستخدم قاعدة الصفحة لتحديد الأبعاد والاتجاه والهوامش وما إلى ذلك لصندوق الصفحة للوسائط المرقّمة."
type: docs

url: /ar/java/com.aspose.html.dom.css/icsspagerule/
---
## ICSSPageRule interface

تمثل واجهة CSSPageRule قاعدة @page داخل ورقة أنماط CSS. تُستخدم قاعدة @page لتحديد أبعاد، اتجاه، هوامش، إلخ لصندوق الصفحة للوسائط المرقّمة.

```java
public interface ICSSPageRule : ICSSRule
```

## الخصائص

| الاسم | الوصف |
| --- | --- |
[getSelectorText]
[setSelectorText] The selectorText property of the `CSSPageRule` interface gets and sets the selectors associated with the `CSSPageRule`. |
| [getStyle](../../com.aspose.html.dom.css/icsspagerule/style/) خاصية النمط للقراءة فقط في واجهة `CSSPageRule` تُعيد كائنًا من نوع [`CSSStyleDeclaration`](../icssstyledeclaration/). هذا يمثل كائنًا هو كتلة إعلان CSS، ويكشف عن معلومات النمط ومختلف الأساليب والخصائص المتعلقة بالنمط. |

### انظر أيضًا

* interface [ICSSRule](../icssrule/)
* package [com.aspose.html.dom.css](../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../)
