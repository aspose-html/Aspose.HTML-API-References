---
title: "واجهة ICSSPageRule"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "واجهة com.aspose.html.dom.css.ICSSPageRule. تمثل واجهة CSSPageRule قاعدة صفحة داخل ورقة أنماط CSS. تُستخدم قاعدة الصفحة لتحديد الأبعاد والاتجاه والهوامش وما إلى ذلك لصندوق الصفحة للوسائط المرقمة."
type: docs

url: /ar/java/com.aspose.html.dom.css/icsspagerule/
---
## ICSSPageRule interface

تمثل واجهة CSSPageRule قاعدة @page داخل ورقة أنماط CSS. تُستخدم قاعدة @page لتحديد الأبعاد، الاتجاه، الهوامش، إلخ، لصندوق الصفحة للوسائط المرقّمة.

```java
public interface ICSSPageRule : ICSSRule
```

## الخصائص

| الاسم | الوصف |
| --- | --- |
[getSelectorText]
[setSelectorText] The selectorText property of the `CSSPageRule` interface gets and sets the selectors associated with the `CSSPageRule`. |
| [getStyle](../../com.aspose.html.dom.css/icsspagerule/style/) الخاصية للقراءة فقط `style` في واجهة `CSSPageRule` تُرجع كائنًا من نوع [`CSSStyleDeclaration`](../icssstyledeclaration/). يمثل هذا كائنًا هو كتلة إعلان CSS، ويكشف عن معلومات الأنماط ومختلف الأساليب والخصائص المتعلقة بالأنماط. |

### انظر أيضًا

* interface [ICSSRule](../icssrule/)
* package [com.aspose.html.dom.css](../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../)
