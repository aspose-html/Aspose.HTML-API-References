---
title: "ICSSMediaRule.InsertRule"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة ICSSMediaRule. طريقة insertRule لواجهة CSSGroupingRule تُضيف قاعدة CSS جديدة إلى قائمة قواعد CSS."
type: docs

url: /ar/java/com.aspose.html.dom.css/icssmediarule/insertrule/
---
## ICSSMediaRule.InsertRule method

طريقة insertRule() في واجهة CSSGroupingRule تضيف قاعدة CSS جديدة إلى قائمة قواعد CSS.

```java
public long InsertRule(String rule, long index)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| قاعدة | String | قاعدة الوسائط كـ CSSOMString. |
| index | Int64 | فهرس اختياري يُحدد موقع إدراج القاعدة؛ القيمة الافتراضية هي 0. |

### قيمة الإرجاع

طريقة insertRule(rule, index) تُعيد نتيجة استدعاء إدراج قاعدة CSS في قواعد CSS الفرعية عند الفهرس المحدد.

### انظر أيضًا

* interface [ICSSMediaRule](../)
* package [com.aspose.html.dom.css](../../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../../)
