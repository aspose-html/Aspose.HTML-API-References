---
title: "FormSubmitter.Submit"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة FormSubmitter. تُرسل بيانات النموذج إلى الخادم"
type: docs

url: /ar/java/com.aspose.html.forms/formsubmitter/submit/
---
## Submit() {#submit}

يرسل بيانات النموذج إلى الخادم.

```java
public SubmissionResult Submit()
```

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(CookieContainer) {#submit_1}

يرسل بيانات النموذج إلى الخادم مع ملفات تعريف الارتباط المحددة.

```java
public SubmissionResult Submit(CookieContainer cookieContainer)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| cookieContainer | CookieContainer | حاوية ملفات تعريف الارتباط. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials) {#submit_2}

يُرسل بيانات النموذج إلى الخادم باستخدام بيانات اعتماد المستخدم المحددة.

```java
public SubmissionResult Submit(ICredentials credentials)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| credentials | ICredentials | معلومات المصادقة للطلب. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, CookieContainer) {#submit_3}

يُرسل بيانات النموذج إلى الخادم باستخدام بيانات اعتماد المستخدم المحددة وملفات تعريف الارتباط.

```java
public SubmissionResult Submit(ICredentials credentials, CookieContainer cookieContainer)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| credentials | ICredentials | معلومات المصادقة للطلب. |
| cookieContainer | CookieContainer | حاوية ملفات تعريف الارتباط. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(TimeSpan) {#submit_8}

يُرسل بيانات النموذج إلى الخادم باستخدام مهلة محددة.

```java
public SubmissionResult Submit(TimeSpan timeout)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| مهلة | TimeSpan | عدد الملليثانية التي يجب الانتظار قبل انتهاء مهلة الطلب. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(TimeSpan, CookieContainer) {#submit_9}

يُرسل بيانات النموذج إلى الخادم باستخدام مهلة محددة وملفات تعريف الارتباط.

```java
public SubmissionResult Submit(TimeSpan timeout, CookieContainer cookieContainer)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| مهلة | TimeSpan | عدد الملليثانية التي يجب الانتظار قبل انتهاء مهلة الطلب. |
| cookieContainer | CookieContainer | حاوية ملفات تعريف الارتباط. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan) {#submit_4}

يُرسل بيانات النموذج إلى الخادم باستخدام بيانات اعتماد المستخدم المحددة والمهلة.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| credentials | ICredentials | معلومات المصادقة للطلب. |
| مهلة | TimeSpan | عدد الملليثانية التي يجب الانتظار قبل انتهاء مهلة الطلب. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan, CookieContainer) {#submit_7}

يُرسل بيانات النموذج إلى الخادم باستخدام بيانات اعتماد المستخدم المحددة، والمهلة، وملفات تعريف الارتباط.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout, 
    CookieContainer cookieContainer)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| credentials | ICredentials | معلومات المصادقة للطلب. |
| مهلة | TimeSpan | عدد الملليثانية التي يجب الانتظار قبل انتهاء مهلة الطلب. |
| cookieContainer | CookieContainer | حاوية ملفات تعريف الارتباط. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan, bool) {#submit_5}

يُرسل بيانات النموذج إلى الخادم باستخدام بيانات اعتماد المستخدم المحددة.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout, bool preAuthenticate)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| credentials | ICredentials | معلومات المصادقة للطلب. |
| مهلة | TimeSpan | عدد الملليثانية التي يجب الانتظار قبل انتهاء مهلة الطلب. |
| preAuthenticate | Boolean | القيمة التي تشير إلى ما إذا كان يجب إرسال رأس Authorization مع الطلب. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan, bool, CookieContainer) {#submit_6}

يُرسل بيانات النموذج إلى الخادم باستخدام بيانات اعتماد المستخدم المحددة وملفات تعريف الارتباط.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout, bool preAuthenticate, 
    CookieContainer cookieContainer)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| credentials | ICredentials | معلومات المصادقة للطلب. |
| مهلة | TimeSpan | عدد الملليثانية التي يجب الانتظار قبل انتهاء مهلة الطلب. |
| preAuthenticate | Boolean | القيمة التي تشير إلى ما إذا كان يجب إرسال رأس Authorization مع الطلب. |
| cookieContainer | CookieContainer | حاوية ملفات تعريف الارتباط. |

### قيمة الإرجاع

نتيجة الإرسال.

### انظر أيضًا

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)
