---
title: "HTMLCanvasElement.ToDataURL"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة HTMLCanvasElement. تُرجع URI بيانات يحتوي على تمثيل للصورة بالتنسيق المحدد بواسطة معامل type، ويكون الافتراضي PNG. الصورة المرجعة بدقة 96 dpi."
type: docs

url: /ar/java/com.aspose.html/htmlcanvaselement/todataurl/
---
## ToDataURL() {#todataurl}

تُرجع URI بيانات يحتوي على تمثيل الصورة بالتنسيق المحدد بواسطة معامل النوع (الافتراضي PNG). الصورة المُرجعة بدقة 96 نقطة في البوصة.

```java
public String ToDataURL()
```

### قيمة الإرجاع

سلسلة DOMString تحتوي على URI البيانات المطلوب.

### انظر أيضًا

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String) {#todataurl_1}

تُرجع URI بيانات يحتوي على تمثيل الصورة بالتنسيق المحدد بواسطة معامل النوع (الافتراضي PNG). الصورة المُرجعة بدقة 96 نقطة في البوصة.

```java
public String ToDataURL(String type)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| النوع | String | سلسلة String تشير إلى تنسيق الصورة. نوع التنسيق الافتراضي هو image/png. |

### قيمة الإرجاع

سلسلة DOMString تحتوي على URI البيانات المطلوب.

### انظر أيضًا

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String, double) {#todataurl_2}

تُرجع URI بيانات يحتوي على تمثيل الصورة بالتنسيق المحدد بواسطة معامل النوع (الافتراضي PNG). الصورة المُرجعة بدقة 96 نقطة في البوصة.

```java
public String ToDataURL(String type, double encoderOptions)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| النوع | String | سلسلة String تشير إلى تنسيق الصورة. نوع التنسيق الافتراضي هو image/png. |
| encoderOptions | Double | رقم Number بين 0 و 1 يشير إلى جودة الصورة إذا كان النوع المطلوب هو image/jpeg أو image/webp. |

### قيمة الإرجاع

سلسلة DOMString تحتوي على URI البيانات المطلوب.

### انظر أيضًا

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)
