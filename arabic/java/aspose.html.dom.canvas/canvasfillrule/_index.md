---
title: "CanvasFillRule تعداد"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "com.aspose.html.dom.canvas.CanvasFillRule تعداد. يُستخدم هذا التعداد لاختيار خوارزمية قاعدة التعبئة التي تُحدِّد ما إذا كانت النقطة داخل أو خارج المسار."
type: docs

url: /ar/java/com.aspose.html.dom.canvas/canvasfillrule/
---
## CanvasFillRule enumeration

يُستخدم هذا التعداد لاختيار خوارزمية قاعدة التعبئة التي تحدد ما إذا كانت النقطة داخل أو خارج المسار.

```java
public enum CanvasFillRule
```

### القيم

| الاسم | القيمة | الوصف |
| --- | --- | --- |
| NonZero | `0` | القيمة "nonzero" تشير إلى قاعدة اللف غير الصفرية، حيث تُعتبر النقطة خارج الشكل إذا كان عدد المرات التي تقاطع فيها خط مستقيم نصف غير نهائي يُرسم من تلك النقطة مسار الشكل في اتجاه واحد يساوي عدد المرات التي يعبر فيها الخط المسار في الاتجاه المعاكس. |
| EvenOdd | `1` | القيمة "evenodd" تشير إلى قاعدة الزوجي-الفردي، حيث تُعتبر النقطة خارج الشكل إذا كان عدد المرات التي يقاطع فيها خط مستقيم نصف غير نهائي يُرسم من تلك النقطة مسار الشكل عددًا زوجيًا. |

### انظر أيضًا

* package [com.aspose.html.dom.canvas](../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../)
