---
title: "واجهة IImageData"
second_title: "مرجع API لـ Aspose.HTML للـ Java"
description: "واجهة com.aspose.html.dom.canvas.IImageData. تنشئ كائن ImageData من Uint8ClampedArray محدد وحجم الصورة التي يحتويها. إذا لم يُعطَ مصفوفة، فإنها تنشئ صورة لمستطيل أسود."
type: docs

url: /ar/java/com.aspose.html.dom.canvas/iimagedata/
---
## IImageData interface

ينشئ كائن ImageData من Uint8ClampedArray المحدد وحجم الصورة التي يحتويها. إذا لم يُعطَ أي مصفوفة، فإنه ينشئ صورة لمستطيل أسود.

```java
public interface IImageData
```

## الخصائص

| الاسم | الوصف |
| --- | --- |
| [getData](../../com.aspose.html.dom.canvas/iimagedata/data/) هو Uint8ClampedArray يمثل مصفوفة أحادية البعد تحتوي على البيانات بترتيب RGBA، بقيم صحيحة بين 0 و 255 (مشمول). |
| [getHeight](../../com.aspose.html.dom.canvas/iimagedata/height/) هو unsigned long يمثل الارتفاع الفعلي، بوحدات البكسل، لعنصر ImageData. |
| [getWidth](../../com.aspose.html.dom.canvas/iimagedata/width/) هو unsigned long يمثل العرض الفعلي، بوحدات البكسل، لعنصر ImageData. |

### انظر أيضًا

* package [com.aspose.html.dom.canvas](../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../)
