---
title: "ICanvasRenderingContext2D.DrawImage"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة ICanvasRenderingContext2D. ترسم الصورة المحددة"
type: docs

url: /ar/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/drawimage/
---
## DrawImage(HTMLImageElement, double, double) {#drawimage_3}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLImageElement image, double dx, double dy)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLImageElement | عنصر HTMLImageElement للرسم في السياق. |
| dx | Double | الإحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dy | Double | الإحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |

### انظر أيضًا

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double) {#drawimage}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLCanvasElement image, double dx, double dy)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLCanvasElement | عنصر HTMLCanvasElement للرسم في السياق. |
| dx | Double | الإحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dy | Double | الإحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |

### انظر أيضًا

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLImageElement, double, double, double, double) {#drawimage_4}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLImageElement image, double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLImageElement | عنصر HTMLImageElement للرسم في السياق. |
| dx | Double | الإحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dy | Double | الإحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double, double, double) {#drawimage_1}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLCanvasElement image, double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLCanvasElement | عنصر HTMLCanvasElement للرسم في السياق. |
| dx | Double | الإحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dy | Double | الإحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLImageElement, double, double, double, double, double, double, double, double) {#drawimage_5}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLImageElement image, double sx, double sy, double sw, double sh, 
    double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLImageElement | عنصر HTMLImageElement للرسم في السياق. |
| sx | Double | الإحداثي X للزاوية العلوية اليسرى للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. |
| sy | Double | الإحداثي Y للزاوية العلوية اليسرى للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. |
| sw | Double | العرض للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. إذا لم يتم تحديده، يُستخدم المستطيل الكامل من الإحداثيات المحددة بـ sx و sy إلى الزاوية السفلية اليمنى للصورة. |
| sh | Double | الارتفاع للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. |
| dx | Double | الإحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dy | Double | الإحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double, double, double, double, double, double, double) {#drawimage_2}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLCanvasElement image, double sx, double sy, double sw, double sh, 
    double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLCanvasElement | عنصر HTMLCanvasElement للرسم في السياق. |
| sx | Double | الإحداثي X للزاوية العلوية اليسرى للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. |
| sy | Double | الإحداثي Y للزاوية العلوية اليسرى للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. |
| sw | Double | العرض للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. إذا لم يتم تحديده، يُستخدم المستطيل الكامل من الإحداثيات المحددة بـ sx و sy إلى الزاوية السفلية اليمنى للصورة. |
| sh | Double | الارتفاع للمستطيل الفرعي للصورة المصدر للرسم في السياق الوجهة. |
| dx | Double | الإحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dy | Double | الإحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العلوية اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لن يتم تحجيم الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
