---
title: "ICanvasRenderingContext2D.DrawImage"
second_title: "مرجع API لـ Aspose.HTML للـ Java"
description: "طريقة ICanvasRenderingContext2D. ترسم الصورة المحددة"
type: docs

url: /ar/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/drawimage/
---
## DrawImage(HTMLImageElement, double, double) {#drawimage_3}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLImageElement image, double dx, double dy)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLImageElement | عنصر HTMLImageElement للرسم داخل السياق. |
| dx | Double | إحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dy | Double | إحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |

### انظر أيضًا

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double) {#drawimage}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLCanvasElement image, double dx, double dy)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLCanvasElement | عنصر HTMLCanvasElement للرسم داخل السياق. |
| dx | Double | إحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dy | Double | إحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |

### انظر أيضًا

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLImageElement, double, double, double, double) {#drawimage_4}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLImageElement image, double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLImageElement | عنصر HTMLImageElement للرسم داخل السياق. |
| dx | Double | إحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dy | Double | إحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double, double, double) {#drawimage_1}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLCanvasElement image, double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLCanvasElement | عنصر HTMLCanvasElement للرسم داخل السياق. |
| dx | Double | إحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dy | Double | إحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLImageElement, double, double, double, double, double, double, double, double) {#drawimage_5}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLImageElement image, double sx, double sy, double sw, double sh, 
    double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLImageElement | عنصر HTMLImageElement للرسم داخل السياق. |
| sx | Double | إحداثي X للزاوية العليا اليسرى للمستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. |
| sy | Double | إحداثي Y للزاوية العليا اليسرى للمستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. |
| sw | Double | عرض المستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. إذا لم يتم تحديده، يُستخدم المستطيل الكامل من الإحداثيات المحددة بواسطة sx و sy إلى الزاوية السفلية اليمنى للصورة. |
| sh | Double | ارتفاع المستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. |
| dx | Double | إحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dy | Double | إحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double, double, double, double, double, double, double) {#drawimage_2}

يرسم الصورة المحددة.

```java
public void DrawImage(HTMLCanvasElement image, double sx, double sy, double sw, double sh, 
    double dx, double dy, double dw, double dh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| صورة | HTMLCanvasElement | عنصر HTMLCanvasElement للرسم داخل السياق. |
| sx | Double | إحداثي X للزاوية العليا اليسرى للمستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. |
| sy | Double | إحداثي Y للزاوية العليا اليسرى للمستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. |
| sw | Double | عرض المستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. إذا لم يتم تحديده، يُستخدم المستطيل الكامل من الإحداثيات المحددة بواسطة sx و sy إلى الزاوية السفلية اليمنى للصورة. |
| sh | Double | ارتفاع المستطيل الفرعي للصورة المصدر للرسم داخل السياق الوجهة. |
| dx | Double | إحداثي X في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dy | Double | إحداثي Y في لوحة الرسم الوجهة حيث يتم وضع الزاوية العليا اليسرى للصورة المصدر. |
| dw | Double | العرض لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في العرض عند الرسم. |
| dh | Double | الارتفاع لرسم الصورة في لوحة الرسم الوجهة. يتيح ذلك تحجيم الصورة المرسومة. إذا لم يتم تحديده، لا يتم تحجيم الصورة في الارتفاع عند الرسم. |

### انظر أيضًا

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
