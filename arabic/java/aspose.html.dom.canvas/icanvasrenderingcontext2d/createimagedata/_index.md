---
title: "ICanvasRenderingContext2D.CreateImageData"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة ICanvasRenderingContext2D. تُنشئ كائن ImageData فارغ جديد بالأبعاد المحددة. جميع البكسلات في الكائن الجديد هي أسود شفاف."
type: docs

url: /ar/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/createimagedata/
---
## CreateImageData(double, double) {#createimagedata_1}

ينشئ كائن ImageData جديد فارغ بالأبعاد المحددة. جميع البكسلات في الكائن الجديد هي باللون الأسود الشفاف.

```java
public IImageData CreateImageData(double sw, double sh)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| sw | Double | العرض الذي سيُعطى لكائن ImageData الجديد. |
| sh | Double | الارتفاع الذي سيُعطى لكائن ImageData الجديد. |

### قيمة الإرجاع

كائن ImageData جديد بالعرض والارتفاع المحددين. يتم ملء الكائن الجديد بكسلات سوداء شفافة.

### انظر أيضًا

* interface [IImageData](../../iimagedata/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## CreateImageData(IImageData) {#createimagedata}

ينشئ كائن ImageData جديد فارغ بالأبعاد المحددة. جميع البكسلات في الكائن الجديد هي باللون الأسود الشفاف.

```java
public IImageData CreateImageData(IImageData imagedata)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| imagedata | IImageData | كائن ImageData موجود يتم منه نسخ العرض والارتفاع. لا يتم نسخ الصورة نفسها. |

### قيمة الإرجاع

كائن ImageData جديد بالعرض والارتفاع المحددين. يتم ملء الكائن الجديد بكسلات سوداء شفافة.

### انظر أيضًا

* interface [IImageData](../../iimagedata/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
