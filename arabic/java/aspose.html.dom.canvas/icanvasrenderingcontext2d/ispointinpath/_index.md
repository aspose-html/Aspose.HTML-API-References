---
title: "ICanvasRenderingContext2D.IsPointInPath"
second_title: "مرجع API لـ Aspose.HTML للـ Java"
description: "طريقة ICanvasRenderingContext2D. تُبلغ ما إذا كانت النقطة المحددة موجودة داخل المسار الحالي أم لا"
type: docs

url: /ar/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/ispointinpath/
---
## IsPointInPath(double, double) {#ispointinpath_2}

يبلغ عما إذا كانت النقطة المحددة موجودة داخل المسار الحالي أم لا.

```java
public bool IsPointInPath(double x, double y)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| x | Double | إحداثي X للنقطة المراد فحصها. |
| y | Double | الإحداثي Y للنقطة المراد فحصها. |

### قيمة الإرجاع

يعيد true إذا كانت النقطة داخل المنطقة التي يشكلها تعبئة مسار، وإلا false.

### انظر أيضًا

* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(double, double, CanvasFillRule) {#ispointinpath_3}

يبلغ عما إذا كانت النقطة المحددة موجودة داخل المسار الحالي أم لا.

```java
public bool IsPointInPath(double x, double y, CanvasFillRule fillRule)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| x | Double | إحداثي X للنقطة المراد فحصها. |
| y | Double | الإحداثي Y للنقطة المراد فحصها. |
| fillRule | CanvasFillRule | الخوارزمية المستخدمة لتحديد ما إذا كانت النقطة داخل مسار أو خارجه. |

### قيمة الإرجاع

يعيد true إذا كانت النقطة داخل المنطقة التي يشكلها تعبئة مسار، وإلا false.

### انظر أيضًا

* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double) {#ispointinpath}

يبلغ عما إذا كانت النقطة المحددة موجودة داخل المسار الحالي أم لا.

```java
public bool IsPointInPath(Path2D path, double x, double y)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| المسار | Path2D | مسار Path2D للفحص. |
| x | Double | إحداثي X للنقطة المراد فحصها. |
| y | Double | الإحداثي Y للنقطة المراد فحصها. |

### قيمة الإرجاع

يعيد true إذا كانت النقطة داخل المنطقة التي يشكلها تعبئة مسار، وإلا false.

### انظر أيضًا

* class [Path2D](../../path2d/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## IsPointInPath(Path2D, double, double, CanvasFillRule) {#ispointinpath_1}

يبلغ عما إذا كانت النقطة المحددة موجودة داخل المسار الحالي أم لا.

```java
public bool IsPointInPath(Path2D path, double x, double y, CanvasFillRule fillRule)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| المسار | Path2D | مسار Path2D للفحص. |
| x | Double | إحداثي X للنقطة المراد فحصها. |
| y | Double | الإحداثي Y للنقطة المراد فحصها. |
| fillRule | CanvasFillRule | الخوارزمية المستخدمة لتحديد ما إذا كانت النقطة داخل مسار أو خارجه. |

### قيمة الإرجاع

يعيد true إذا كانت النقطة داخل المنطقة التي يشكلها تعبئة مسار، وإلا false.

### انظر أيضًا

* class [Path2D](../../path2d/)
* enum [CanvasFillRule](../../canvasfillrule/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
