---
title: "SVGSVGElement.SetCurrentTime"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "طريقة SVGSVGElement. تضبط الساعة لهذا الجزء من مستند SVG لتحديد وقت حالي جديد. إذا تم استدعاء setCurrentTime قبل بدء خط الزمن للمستند، على سبيل المثال بواسطة سكريبت يُنفّذ في عنصر script قبل إرسال حدث SVGLoad للمستند، فإن قيمة الثواني في آخر استدعاء للطريقة تعطي الوقت الذي سيتجه إليه المستند بمجرد بدء خط الزمن للمستند."
type: docs

url: /ar/java/com.aspose.html.dom.svg/svgsvgelement/setcurrenttime/
---
## SVGSVGElement.SetCurrentTime method

يضبط الساعة لهذا الجزء من مستند SVG، مُنشئًا وقتًا حاليًا جديدًا. إذا تم استدعاء setCurrentTime قبل بدء خط الزمن للمستند (على سبيل المثال، بواسطة سكريبت يعمل في عنصر ‘script’ قبل إرسال حدث SVGLoad للمستند)، فإن قيمة الثواني في آخر استدعاء للطريقة تعطي الوقت الذي سيتجه إليه المستند بمجرد بدء خط الزمن للمستند.

```java
public void SetCurrentTime(float seconds)
```

| معامل | نوع | الوصف |
| --- | --- | --- |
| ثوانٍ | Single | الوقت الحالي الجديد بالثواني بالنسبة لوقت البدء للجزء الحالي من مستند SVG. |

### انظر أيضًا

* class [SVGSVGElement](../)
* package [com.aspose.html.dom.svg](../../../com.aspose.html.dom.svg/)
* package [Aspose.HTML](../../../)
