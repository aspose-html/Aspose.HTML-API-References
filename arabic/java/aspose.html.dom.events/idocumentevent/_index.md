---
title: "واجهة IDocumentEvent"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "واجهة com.aspose.html.dom.events.IDocumentEvent. تُوفر واجهة DocumentEvent آلية يمكن للمستخدم من خلالها إنشاء حدث من نوع يدعمه التنفيذ. من المتوقع أن تُنفَّذ واجهة DocumentEvent على نفس الكائن الذي يُنفّذ واجهة Document في تنفيذ يدعم نموذج الأحداث."
type: docs

url: /ar/java/com.aspose.html.dom.events/idocumentevent/
---
## IDocumentEvent interface

توفر واجهة DocumentEvent آلية يمكن من خلالها للمستخدم إنشاء حدث من نوع يدعمه التنفيذ. من المتوقع أن تُطبق واجهة DocumentEvent على نفس الكائن الذي يطبق واجهة Document في تنفيذ يدعم نموذج الأحداث.

```java
public interface IDocumentEvent
```

## الطرق

| الاسم | الوصف |
| --- | --- |
| [createEvent](../../com.aspose.html.dom.events/idocumentevent/createevent/)(String) | تُستخدم طريقة createEvent في إنشاء الأحداث عندما يكون ذلك غير ملائم أو غير ضروري للمستخدم لإنشاء الحدث بنفسه. |

## ملاحظات

من المتوقع أن تُنفَّذ الواجهة على نفس الكائن الذي يُنفّذ الواجهة في تنفيذ يدعم نموذج الأحداث.

### انظر أيضًا

* package [com.aspose.html.dom.events](../../com.aspose.html.dom.events/)
* package [Aspose.HTML](../../)
