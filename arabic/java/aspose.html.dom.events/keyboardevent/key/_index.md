---
title: "KeyboardEvent.Key"
second_title: "مرجع API لـ Aspose.HTML for Java"
description: "خاصية KeyboardEvent. يحتوي المفتاح على قيمة المفتاح الذي تم ضغطه. إذا كان للقيمة تمثيل مطبوع يجب أن تكون سلسلة Unicode غير فارغة تتوافق مع الخوارزمية المحددة لتحديد قيمة المفتاح في هذه المواصفة. إذا كانت القيمة مفتاح تحكم لا يحتوي على تمثيل مطبوع يجب أن تكون إحدى قيم المفاتيح المعرفة في مجموعة قيم المفاتيح كما تحددها الخوارزمية لتحديد قيمة المفتاح. يجب على التطبيقات التي لا تستطيع تحديد المفتاح استخدام قيمة المفتاح Unidentified."
type: docs

url: /ar/java/com.aspose.html.dom.events/keyboardevent/key/
---
## KeyboardEvent.Key property

المفتاح يحتوي على قيمة المفتاح الذي تم ضغطه. إذا كان للقيمة تمثيل مطبوع، يجب أن تكون سلسلة Unicode غير فارغة، متوافقة مع الخوارزمية المحددة لتحديد قيمة المفتاح في هذه المواصفة. إذا كانت القيمة مفتاح تحكم لا يحتوي على تمثيل مطبوع، يجب أن تكون إحدى قيم المفاتيح المعرفة في مجموعة قيم المفاتيح، كما تحددها الخوارزمية لتحديد قيمة المفتاح. يجب على التطبيقات التي لا تستطيع تحديد المفتاح استخدام قيمة المفتاح Unidentified.

```java
public String Key { get; }
```

### Property Value

قيمة المفتاح.

### انظر أيضًا

* class [KeyboardEvent](../)
* package [com.aspose.html.dom.events](../../../com.aspose.html.dom.events/)
* package [Aspose.HTML](../../../)
