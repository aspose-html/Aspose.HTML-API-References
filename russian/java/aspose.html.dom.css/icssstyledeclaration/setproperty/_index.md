---
title: "ICSSStyleDeclaration.SetProperty"
second_title: "Справочник API Aspose.HTML для Java"
description: "Метод ICSSStyleDeclaration. Интерфейс метода CSSStyleDeclaration.setProperty используется для установки значения свойства с приоритетом по умолчанию в этом блоке объявлений. Приоритет по умолчанию не важен, т.д. String.Empty."
type: docs

url: /ru/java/com.aspose.html.dom.css/icssstyledeclaration/setproperty/
---
## SetProperty(String, String) {#setproperty}

Метод интерфейса CSSStyleDeclaration.setProperty() используется для установки значения свойства с приоритетом по умолчанию в этом блоке объявлений. Приоритет по умолчанию не является "important", т.е. String.Empty.

```java
public void SetProperty(String propertyName, String value)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| propertyName | String | Строка, представляющая имя CSS‑свойства (в виде с дефисом), которое следует изменить. |
| значение | String | Строка, содержащая новое значение свойства. Если не указано, считается пустой строкой. |

### См. также

* interface [ICSSStyleDeclaration](../)
* package [com.aspose.html.dom.css](../../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../../)

---

## SetProperty(String, String, String) {#setproperty_1}

Метод интерфейса CSSStyleDeclaration.setProperty() используется для установки значения свойства с приоритетом по умолчанию в этом блоке объявлений. Приоритет по умолчанию не является "important", т.е. String.Empty.

```java
public void SetProperty(String propertyName, String value, String priority)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| propertyName | String | Строка, представляющая имя CSS‑свойства (в виде с дефисом), которое следует изменить. |
| значение | String | Строка, содержащая новое значение свойства. Если не указано, считается пустой строкой. |
| priority | String | Строка, позволяющая установить CSS‑приоритет "important". Если не указано, считается пустой строкой. Допустимы следующие значения: строковое значение "important", ключевое слово undefined или пустая строка "". |

### См. также

* interface [ICSSStyleDeclaration](../)
* package [com.aspose.html.dom.css](../../../com.aspose.html.dom.css/)
* package [Aspose.HTML](../../../)
