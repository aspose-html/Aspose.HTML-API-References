---
title: "Document.Write"
second_title: "Справочник API Aspose.HTML для Java"
description: "Метод Document. Записывает String текста в поток документа, открытый функцией open. Обратите внимание, что функция создаст документ, который не обязательно управляется DTD, и поэтому может привести к недействительному результату в контексте документа."
type: docs

url: /ru/java/com.aspose.html.dom/document/write/
---
## Document.Write method

Записывает строку текста в поток документа, открытый методом open(). Обратите внимание, что функция создаст документ, который не обязательно управляется DTD, и поэтому может дать недопустимый результат в контексте документа.

```java
public void Write(params String[] texts)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| тексты | String[] | Строка, которую необходимо разобрать в некоторую структуру модели структуры документа. |

### См. также

* class [Document](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
