---
title: "Перечисление CanvasFillRule"
second_title: "Справочник API Aspose.HTML для Java"
description: "Перечисление com.aspose.html.dom.canvas.CanvasFillRule. Это перечисление используется для выбора алгоритма правила заливки, с помощью которого определяется, находится ли точка внутри или снаружи пути."
type: docs

url: /ru/java/com.aspose.html.dom.canvas/canvasfillrule/
---
## CanvasFillRule enumeration

Это перечисление используется для выбора алгоритма правила заполнения, определяющего, находится ли точка внутри или снаружи пути.

```java
public enum CanvasFillRule
```

### Значения

| Имя | Значение | Описание |
| --- | --- | --- |
| NonZero | `0` | Значение "nonzero" указывает на правило ненулевого winding, при котором точка считается находящейся снаружи фигуры, если количество пересечений полубесконечной прямой, проведённой из этой точки, с контуром фигуры в одном направлении равно количеству пересечений в противоположном направлении. |
| EvenOdd | `1` | Значение "evenodd" указывает на правило чёт-нечёт, при котором точка считается находящейся снаружи фигуры, если количество пересечений полубесконечной прямой, проведённой из этой точки, с контуром фигуры является чётным. |

### См. также

* package [com.aspose.html.dom.canvas](../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../)
