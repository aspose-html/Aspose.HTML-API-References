---
title: "IEventTarget.DispatchEvent"
second_title: "Справочник API Aspose.HTML для Java"
description: "Метод IEventTarget. Отправляет событие (Event) на указанный EventTarget синхронно, вызывая затронутые EventListeners в соответствующем порядке. Обычные правила обработки событий, включая фазу захвата и необязательную фазу всплытия, также применяются к событиям, отправленным вручную с помощью dispatchEvent."
type: docs

url: /ru/java/com.aspose.html.dom.events/ieventtarget/dispatchevent/
---
## IEventTarget.DispatchEvent method

Отправляет событие (Event) к указанному EventTarget (синхронно), вызывая затронутые EventListeners в соответствующем порядке. Обычные правила обработки событий (включая фазу захвата и необязательную фазу всплытия) также применяются к событиям, отправленным вручную с помощью dispatchEvent().

```java
public bool DispatchEvent(Event @event)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| событие | Событие | Указывает тип события, его поведение и контекстную информацию, которые будут использоваться при обработке события. |

### Возвращаемое значение

Возвращаемое значение указывает, был ли вызван какой-либо из слушателей, обработавших событие. Если вызов был выполнен, значение — false, иначе — true.

### Исключения

| исключение | условие |
| --- | --- |
| [dOMException](../../../com.aspose.html.dom/domexception/) | Исключения, выброшенные обработчиками событий, сообщаются как необработанные исключения. Обработчики событий выполняются в вложенном стеке вызовов; они блокируют вызывающего до завершения, но исключения не распространяются на вызывающего. |

## Примечания

События, отправленные таким способом, будут иметь такое же поведение захвата и всплытия, как события, отправленные напрямую реализацией. Целевым объектом события является объект, на котором вызывается.

### См. также

* class [Event](../../event/)
* interface [IEventTarget](../)
* package [com.aspose.html.dom.events](../../../com.aspose.html.dom.events/)
* package [Aspose.HTML](../../../)
