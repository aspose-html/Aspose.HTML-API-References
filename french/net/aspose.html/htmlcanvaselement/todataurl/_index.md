---
title: ToDataURL
second_title: Référence de l'API Aspose.HTML pour .NET
description: renvoie un URI de données contenant une représentation de limage dans le format spécifié par le paramètre type par défaut PNG. Limage renvoyée est dans une résolution de 96 dpi.
type: docs
weight: 40
url: /fr/net/aspose.html/htmlcanvaselement/todataurl/
---
## ToDataURL() {#todataurl}

renvoie un URI de données contenant une représentation de l'image dans le format spécifié par le paramètre type (par défaut, PNG). L'image renvoyée est dans une résolution de 96 dpi.

```csharp
public string ToDataURL()
```

### Return_Value

Un DOMString contenant l'URI des données demandées.

### Voir également

* class [HTMLCanvasElement](../../htmlcanvaselement)
* espace de noms [Aspose.Html](../../htmlcanvaselement)
* Assemblée [Aspose.HTML](../../../)

---

## ToDataURL(string) {#todataurl_1}

renvoie un URI de données contenant une représentation de l'image dans le format spécifié par le paramètre type (par défaut, PNG). L'image renvoyée est dans une résolution de 96 dpi.

```csharp
public string ToDataURL(string type)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| type | String | Une chaîne indiquant le format de l'image. Le type de format par défaut est image/png. |

### Return_Value

Un DOMString contenant l'URI des données demandées.

### Voir également

* class [HTMLCanvasElement](../../htmlcanvaselement)
* espace de noms [Aspose.Html](../../htmlcanvaselement)
* Assemblée [Aspose.HTML](../../../)

---

## ToDataURL(string, double) {#todataurl_2}

renvoie un URI de données contenant une représentation de l'image dans le format spécifié par le paramètre type (par défaut, PNG). L'image renvoyée est dans une résolution de 96 dpi.

```csharp
public string ToDataURL(string type, double encoderOptions)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| type | String | Une chaîne indiquant le format de l'image. Le type de format par défaut est image/png. |
| encoderOptions | Double | Un nombre compris entre 0 et 1 indiquant la qualité de l'image si le type demandé est image/jpeg ou image/webp. |

### Return_Value

Un DOMString contenant l'URI des données demandées.

### Voir également

* class [HTMLCanvasElement](../../htmlcanvaselement)
* espace de noms [Aspose.Html](../../htmlcanvaselement)
* Assemblée [Aspose.HTML](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.HTML.dll -->
