---
title: "SVGPathElement.AnimatedPathSegList"
second_title: "Référence de l'API Aspose.HTML pour Java"
description: "Propriété SVGPathElement. Fournit l'accès au contenu animé actuel de l'attribut d sous une forme qui correspond un à un à la syntaxe SVG. Si l'attribut ou la propriété donné(e) est en cours d'animation, contient la valeur animée actuelle de l'attribut ou de la propriété et l'objet ainsi que son contenu sont en lecture seule. Si l'attribut ou la propriété donné(e) n'est pas actuellement animé, contient la même valeur que pathSegList"
type: docs

url: /fr/java/com.aspose.html.dom.svg/svgpathelement/animatedpathseglist/
---
## SVGPathElement.AnimatedPathSegList property

Fournit l'accès au contenu animé actuel de l'attribut ‘d’ sous une forme qui correspond un à un à la syntaxe SVG. Si l'attribut ou la propriété donné(e) est animé(e), il contient la valeur animée actuelle de l'attribut ou de la propriété, et l'objet ainsi que son contenu sont en lecture seule. Si l'attribut ou la propriété donné(e) n'est pas actuellement animé(e), il contient la même valeur que pathSegList.

```java
public SVGPathSegList AnimatedPathSegList { get; }
```

### Property Value

La liste de segments de chemin animée.

### Voir aussi

* class [SVGPathSegList](../../../com.aspose.html.dom.svg.paths/svgpathseglist/)
* class [SVGPathElement](../)
* package [com.aspose.html.dom.svg](../../../com.aspose.html.dom.svg/)
* package [Aspose.HTML](../../../)
