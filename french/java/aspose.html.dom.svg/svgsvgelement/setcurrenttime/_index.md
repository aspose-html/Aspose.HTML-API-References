---
title: "SVGSVGElement.SetCurrentTime"
second_title: "Référence de l'API Aspose.HTML pour Java"
description: "Méthode SVGSVGElement. Ajuste l'horloge de ce fragment de document SVG en établissant un nouveau temps actuel. Si setCurrentTime est appelé avant que la chronologie du document n'ait commencé, par exemple par un script s'exécutant dans un élément script avant que l'événement SVGLoad du document ne soit déclenché, alors la valeur en secondes lors de la dernière invocation de la méthode indique le moment auquel le document se déplacera une fois que la chronologie du document aura commencé."
type: docs

url: /fr/java/com.aspose.html.dom.svg/svgsvgelement/setcurrenttime/
---
## SVGSVGElement.SetCurrentTime method

Ajuste l’horloge pour ce fragment de document SVG, établissant un nouveau temps actuel. Si setCurrentTime est appelé avant que la chronologie du document n’ait commencé (par exemple, par un script s’exécutant dans un élément ‘script’ avant que l’événement SVGLoad du document ne soit déclenché), alors la valeur des secondes lors de la dernière invocation de la méthode indique le moment auquel le document cherchera à se placer une fois la chronologie du document commencée.

```java
public void SetCurrentTime(float seconds)
```

| Paramètre | Type | Description |
| --- | --- | --- |
| secondes | Single | Le nouveau temps actuel en secondes, relatif au temps de départ du fragment de document SVG actuel. |

### Voir aussi

* class [SVGSVGElement](../)
* package [com.aspose.html.dom.svg](../../../com.aspose.html.dom.svg/)
* package [Aspose.HTML](../../../)
