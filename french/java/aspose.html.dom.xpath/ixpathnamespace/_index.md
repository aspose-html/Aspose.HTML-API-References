---
title: "Interface IXPathNamespace"
second_title: "Référence de l'API Aspose.HTML pour Java"
description: "interface com.aspose.html.dom.xpath.IXPathNamespace. L'interface XPathNamespace est renvoyée par les interfaces XPathResult pour représenter le type de nœud de package XPath qui manque dans le DOM"
type: docs

url: /fr/java/com.aspose.html.dom.xpath/ixpathpackage/
---
## IXPathNamespace interface

L’interface XPathNamespace est renvoyée par les interfaces XPathResult pour représenter le type de nœud du package XPath qui manque dans le DOM.

```java
public interface IXPathNamespace
```

## Propriétés

| Nom | Description |
| --- | --- |
| [getOwnerElement](../../com.aspose.html.dom.xpath/ixpathpackage/ownerelement/) L'[`Element`](../../com.aspose.html.dom/element/) sur lequel le package était en portée lorsqu'il a été demandé. Cela ne change pas sur un nœud de package retourné même si le document change de sorte que le package sort de la portée sur cet élément et que ce nœud n'est plus trouvé par XPath. |

### Voir aussi

* package [com.aspose.html.dom.xpath](../../com.aspose.html.dom.xpath/)
* package [Aspose.HTML](../../)
