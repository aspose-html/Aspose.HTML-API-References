---
title: "ITreeWalker.NextNode"
second_title: "Référence de l'API Aspose.HTML pour Java"
description: "Méthode ITreeWalker. Déplace le TreeWalker vers le nœud visible suivant dans l'ordre du document par rapport au nœud actuel et renvoie le nouveau nœud. Si le nœud actuel n'a pas de nœud suivant ou si la recherche de nextNode tente de remonter depuis le nœud racine du TreeWalker, renvoie null et conserve le nœud actuel"
type: docs

url: /fr/java/com.aspose.html.dom.traversal/itreewalker/nextnode/
---
## ITreeWalker.NextNode method

Déplace le TreeWalker vers le nœud visible suivant dans l’ordre du document par rapport au nœud actuel et renvoie le nouveau nœud. Si le nœud actuel n’a pas de nœud suivant, ou si la recherche de nextNode tente de remonter depuis le nœud racine du TreeWalker, renvoie null et conserve le nœud actuel.

```java
public Node NextNode()
```

### Valeur de retour

Le nouveau nœud, ou null si le nœud actuel n'a pas de nœud suivant dans la vue logique du TreeWalker.

### Voir aussi

* class [Node](../../../com.aspose.html.dom/node/)
* interface [ITreeWalker](../)
* package [com.aspose.html.dom.traversal](../../../com.aspose.html.dom.traversal/)
* package [Aspose.HTML](../../../)
