---
title: "Text.ReplaceWholeText"
second_title: "Référence de l'API Aspose.HTML pour Java"
description: "Méthode Text. Remplace le texte du nœud actuel et de tous les nœuds texte logiquement adjacents par le texte spécifié. Tous les nœuds texte logiquement adjacents sont supprimés, y compris le nœud actuel, sauf s'il était le destinataire du texte de remplacement."
type: docs

url: /fr/java/com.aspose.html.dom/text/replacewholetext/
---
## Text.ReplaceWholeText method

Remplace le texte du nœud actuel et de tous les nœuds texte logiquement adjacents par le texte spécifié. Tous les nœuds texte logiquement adjacents sont supprimés, y compris le nœud actuel, à moins qu'il n'ait été le destinataire du texte de remplacement.

```java
public Text ReplaceWholeText(String content)
```

| Paramètre | Type | Description |
| --- | --- | --- |
| contenu | String | Le contenu. |

### Valeur de retour

Le [`Text`](../).

### Voir aussi

* class [Text](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
