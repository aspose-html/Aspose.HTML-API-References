---
title: "Node.ReplaceChild"
second_title: "Référence de l'API Aspose.HTML pour Java"
description: "Méthode Node. Remplace le nœud enfant oldChild par newChild dans la liste des enfants et renvoie le nœud oldChild. Si newChild est un objet DocumentFragment, oldChild est remplacé par tous les enfants du DocumentFragment qui sont insérés dans le même ordre. Si newChild est déjà dans l'arbre, il est d'abord supprimé."
type: docs

url: /fr/java/com.aspose.html.dom/node/replacechild/
---
## Node.ReplaceChild method

Remplace le nœud enfant oldChild par newChild dans la liste des enfants, et renvoie le nœud oldChild. Si newChild est un objet [`DocumentFragment`](../../documentfragment/), oldChild est remplacé par tous les enfants du [`DocumentFragment`](../../documentfragment/) qui sont insérés dans le même ordre. Si newChild est déjà dans l'arbre, il est d'abord supprimé.

```java
public Node ReplaceChild(Node node, Node child)
```

| Paramètre | Type | Description |
| --- | --- | --- |
| nœud | Node | Le nouveau nœud pour remplacer oldChild. |
| enfant | Node | L'enfant à remplacer. |

### Valeur de retour

Le nœud remplacé. Il s'agit du même nœud que oldChild.

### Voir aussi

* class [Node](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
