---
title: "IBlob.Slice"
second_title: "Aspose.HTML per Java Riferimento API"
description: "Metodo IBlob. Restituisce un nuovo oggetto Blob con byte compresi tra il parametro opzionale start e, ma non includendo, il parametro opzionale end, e con un attributo type il cui valore è quello del parametro opzionale contentType."
type: docs

url: /it/java/com.aspose.html.io/iblob/slice/
---
## IBlob.Slice method

Restituisce un nuovo oggetto Blob con byte che vanno dal parametro opzionale start fino, ma non includendo, il parametro opzionale end, e con un attributo type il cui valore è quello del parametro opzionale contentType.

```java
public IBlob Slice(ulong start, ulong end, String contentType)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| inizio | UInt64 | Il parametro è un valore per il punto di inizio di una slice. |
| fine | UInt64 | Il parametro è un valore per il punto di inizio di una slice. |
| contentType | String | Il parametro è un tipo media del Blob. |

### Valore di ritorno

Restituisce un nuovo oggetto Blob con byte che vanno dal parametro opzionale start fino, ma non includendo, il parametro opzionale end, e con un attributo type il cui valore è quello del parametro opzionale contentType.

### Vedi anche

* interface [IBlob](../)
* package [com.aspose.html.io](../../../com.aspose.html.io/)
* package [Aspose.HTML](../../../)
