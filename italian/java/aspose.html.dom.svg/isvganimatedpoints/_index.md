---
title: "Interfaccia ISVGAnimatedPoints"
second_title: "Riferimento API Aspose.HTML per Java"
description: "com.aspose.html.dom.svg.ISVGAnimatedPoints interfaccia. L'interfaccia SVGAnimatedPoints supporta gli elementi che possiedono un attributo points che contiene un elenco di valori di coordinate e che consentono di animare tale attributo. Inoltre, l'attributo points sull'elemento originale, accessibile tramite l'XML DOM, ad esempio usando la chiamata al metodo getAttribute, rifletterà qualsiasi modifica apportata a points."
type: docs

url: /it/java/com.aspose.html.dom.svg/isvganimatedpoints/
---
## ISVGAnimatedPoints interface

L'interfaccia SVGAnimatedPoints supporta gli elementi che hanno un attributo ‘points’ che contiene un elenco di valori di coordinate e che supportano la possibilità di animare tale attributo. Inoltre, l'attributo ‘points’ sull'elemento originale accessibile tramite l'XML DOM (ad es., usando la chiamata al metodo getAttribute()) rifletterà qualsiasi modifica apportata ai punti.

```java
public interface ISVGAnimatedPoints
```

## Proprietà

| Nome | Descrizione |
| --- | --- |
| [getAnimatedPoints](../../com.aspose.html.dom.svg/isvganimatedpoints/animatedpoints/) Fornisce l'accesso ai contenuti animati attuali dell'attributo ‘points’. Se l'attributo o la proprietà specificata è in animazione, contiene il valore animato corrente dell'attributo o della proprietà. Se l'attributo o la proprietà non è attualmente animata, contiene lo stesso valore di points. |
| [getPoints](../../com.aspose.html.dom.svg/isvganimatedpoints/points/) Fornisce l'accesso ai contenuti di base (cioè statici) dell'attributo ‘points’. |

### Vedi anche

* package [com.aspose.html.dom.svg](../../com.aspose.html.dom.svg/)
* package [Aspose.HTML](../../)
