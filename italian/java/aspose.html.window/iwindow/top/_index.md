---
title: "IWindow.Top"
second_title: "Riferimento API Aspose.HTML per Java"
description: "IWindow property. L'attributo IDL top sull'oggetto Window di un Document in un contesto di navigazione b deve restituire l'oggetto WindowProxy del suo contesto di navigazione di livello superiore, che sarebbe il proprio oggetto WindowProxy se fosse un contesto di livello superiore, se ne ha uno o il proprio oggetto WindowProxy altrimenti, ad es. se fosse un contesto di navigazione annidato staccato"
type: docs

url: /it/java/com.aspose.html.window/iwindow/top/
---
## IWindow.Top property

L'attributo IDL top sull'oggetto Window di un Document in un contesto di navigazione b deve restituire l'oggetto WindowProxy del suo contesto di navigazione di livello superiore (che sarebbe il proprio oggetto WindowProxy se fosse un contesto di livello superiore), se ne ha uno, oppure il proprio oggetto WindowProxy altrimenti (ad es. se fosse un contesto di navigazione annidato staccato).

```java
public IWindow Top { get; }
```

### Vedi anche

* interface [IWindow](../)
* package [com.aspose.html.window](../../../com.aspose.html.window/)
* package [Aspose.HTML](../../../)
