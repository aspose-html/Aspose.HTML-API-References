---
title: "IWindow.Parent"
second_title: "Riferimento API Aspose.HTML per Java"
description: "Proprietà IWindow. L'attributo IDL parent sull'oggetto Window di un Document in un contesto di navigazione b deve restituire l'oggetto WindowProxy del contesto di navigazione padre, se ne esiste uno (cioè se b è un contesto di navigazione figlio), o l'oggetto WindowProxy del contesto di navigazione b stesso, altrimenti (cioè se è un contesto di navigazione di livello superiore o un contesto di navigazione annidato separato)"
type: docs

url: /it/java/com.aspose.html.window/iwindow/parent/
---
## IWindow.Parent property

L'attributo IDL parent sull'oggetto Window di un Document in un contesto di navigazione b deve restituire l'oggetto WindowProxy del contesto di navigazione padre, se ne esiste uno (cioè se b è un contesto di navigazione figlio), oppure l'oggetto WindowProxy del contesto di navigazione b stesso, altrimenti (cioè se è un contesto di navigazione di livello superiore o un contesto di navigazione annidato separato).

```java
public IWindow Parent { get; }
```

### Property Value

Il genitore.

### Vedi anche

* interface [IWindow](../)
* package [com.aspose.html.window](../../../com.aspose.html.window/)
* package [Aspose.HTML](../../../)
