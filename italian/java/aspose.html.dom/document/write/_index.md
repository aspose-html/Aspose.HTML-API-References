---
title: "Document.Write"
second_title: "Aspose.HTML per Java Riferimento API"
description: "Metodo Document. Scrive una stringa di testo in un flusso di documento aperto da open. Nota che la funzione produrrà un documento che non è necessariamente guidato da un DTD e quindi potrebbe generare un risultato non valido nel contesto del documento."
type: docs

url: /it/java/com.aspose.html.dom/document/write/
---
## Document.Write method

Scrivi una stringa di testo in un flusso di documento aperto con open(). Nota che la funzione produrrà un documento che non è necessariamente guidato da un DTD e quindi potrebbe generare un risultato non valido nel contesto del documento.

```java
public void Write(params String[] texts)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| testi | String[] | La String da analizzare in una qualche struttura nel modello di struttura del documento. |

### Vedi anche

* class [Document](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
