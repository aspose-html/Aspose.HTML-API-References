---
title: "Node.IsEqualNode"
second_title: "Aspose.HTML per Java Riferimento API"
description: "Metodo Node. Il metodo isEqualNode dell'interfaccia Node verifica se due nodi sono uguali. Due nodi sono uguali quando hanno lo stesso tipo e le caratteristiche distintive; per gli elementi ciò includerebbe il loro ID, il numero di figli e così via, i loro attributi corrispondono, ecc. Il set specifico di dati che deve corrispondere varia a seconda dei tipi dei nodi."
type: docs

url: /it/java/com.aspose.html.dom/node/isequalnode/
---
## Node.IsEqualNode method

Il metodo isEqualNode() dell'interfaccia [`Node`](../) verifica se due nodi sono uguali. Due nodi sono uguali quando hanno lo stesso tipo, caratteristiche distintive (per gli elementi, ciò includerebbe il loro ID, il numero di figli e così via), i loro attributi corrispondono, ecc. Il set specifico di dati che deve corrispondere varia a seconda dei tipi dei nodi.

```java
public bool IsEqualNode(Node otherNode)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| otherNode | Node | Il [`Node`](../) con cui confrontare l'uguaglianza. |

### Valore di ritorno

Un valore booleano che è true se i due nodi sono uguali, o false altrimenti. Se otherNode è null, isEqualNode() restituisce sempre false.

### Vedi anche

* class [Node](../)
* package [com.aspose.html.dom](../../../com.aspose.html.dom/)
* package [Aspose.HTML](../../../)
