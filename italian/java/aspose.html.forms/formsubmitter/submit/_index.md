---
title: "FormSubmitter.Submit"
second_title: "Aspose.HTML per Java Riferimento API"
description: "FormSubmitter metodo. Invia i dati del modulo al server"
type: docs

url: /it/java/com.aspose.html.forms/formsubmitter/submit/
---
## Submit() {#submit}

Invia i dati del modulo al server.

```java
public SubmissionResult Submit()
```

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(CookieContainer) {#submit_1}

Invia i dati del modulo al server con i cookie specificati.

```java
public SubmissionResult Submit(CookieContainer cookieContainer)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| cookieContainer | CookieContainer | Il contenitore dei cookie. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials) {#submit_2}

Invia i dati del modulo al server con le credenziali utente specificate.

```java
public SubmissionResult Submit(ICredentials credentials)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| credentials | ICredentials | Le informazioni di autenticazione per la richiesta. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, CookieContainer) {#submit_3}

Invia i dati del modulo al server con le credenziali utente e i cookie specificati.

```java
public SubmissionResult Submit(ICredentials credentials, CookieContainer cookieContainer)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| credentials | ICredentials | Le informazioni di autenticazione per la richiesta. |
| cookieContainer | CookieContainer | Il contenitore dei cookie. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(TimeSpan) {#submit_8}

Invia i dati del modulo al server con il timeout specificato.

```java
public SubmissionResult Submit(TimeSpan timeout)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| timeout | TimeSpan | Il numero di millisecondi da attendere prima che la richiesta scada. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(TimeSpan, CookieContainer) {#submit_9}

Invia i dati del modulo al server con il timeout e i cookie specificati.

```java
public SubmissionResult Submit(TimeSpan timeout, CookieContainer cookieContainer)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| timeout | TimeSpan | Il numero di millisecondi da attendere prima che la richiesta scada. |
| cookieContainer | CookieContainer | Il contenitore dei cookie. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan) {#submit_4}

Invia i dati del modulo al server con le credenziali utente e il timeout specificati.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| credentials | ICredentials | Le informazioni di autenticazione per la richiesta. |
| timeout | TimeSpan | Il numero di millisecondi da attendere prima che la richiesta scada. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan, CookieContainer) {#submit_7}

Invia i dati del modulo al server con le credenziali utente, il timeout e i cookie specificati.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout, 
    CookieContainer cookieContainer)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| credentials | ICredentials | Le informazioni di autenticazione per la richiesta. |
| timeout | TimeSpan | Il numero di millisecondi da attendere prima che la richiesta scada. |
| cookieContainer | CookieContainer | Il contenitore dei cookie. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan, bool) {#submit_5}

Invia i dati del modulo al server con le credenziali utente specificate.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout, bool preAuthenticate)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| credentials | ICredentials | Le informazioni di autenticazione per la richiesta. |
| timeout | TimeSpan | Il numero di millisecondi da attendere prima che la richiesta scada. |
| preAuthenticate | Boolean | Il valore che indica se inviare un'intestazione Authorization con la richiesta. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)

---

## Submit(ICredentials, TimeSpan, bool, CookieContainer) {#submit_6}

Invia i dati del modulo al server con le credenziali utente e i cookie specificati.

```java
public SubmissionResult Submit(ICredentials credentials, TimeSpan timeout, bool preAuthenticate, 
    CookieContainer cookieContainer)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| credentials | ICredentials | Le informazioni di autenticazione per la richiesta. |
| timeout | TimeSpan | Il numero di millisecondi da attendere prima che la richiesta scada. |
| preAuthenticate | Boolean | Il valore che indica se inviare un'intestazione Authorization con la richiesta. |
| cookieContainer | CookieContainer | Il contenitore dei cookie. |

### Valore di ritorno

Il risultato dell'invio.

### Vedi anche

* class [SubmissionResult](../../submissionresult/)
* class [FormSubmitter](../)
* package [com.aspose.html.forms](../../../com.aspose.html.forms/)
* package [Aspose.HTML](../../../)
