---
title: "HTMLCanvasElement.ToDataURL"
second_title: "Aspose.HTML för Java API-referens"
description: "HTMLCanvasElement method. returnerar en data URI som innehåller en representation av bilden i det format som anges av type‑parametern, standard är PNG. Den returnerade bilden har en upplösning på 96 dpi"
type: docs

url: /sv/java/com.aspose.html/htmlcanvaselement/todataurl/
---
## ToDataURL() {#todataurl}

returnerar en data‑URI som innehåller en representation av bilden i det format som anges av typ‑parametern (standard är PNG). Den returnerade bilden har en upplösning på 96 dpi.

```java
public String ToDataURL()
```

### Returvärde

En DOMString som innehåller den begärda data‑URI:n.

### Se även

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String) {#todataurl_1}

returnerar en data‑URI som innehåller en representation av bilden i det format som anges av typ‑parametern (standard är PNG). Den returnerade bilden har en upplösning på 96 dpi.

```java
public String ToDataURL(String type)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| typ | String | En String som indikerar bildformatet. Standardformatet är image/png. |

### Returvärde

En DOMString som innehåller den begärda data‑URI:n.

### Se även

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)

---

## ToDataURL(String, double) {#todataurl_2}

returnerar en data‑URI som innehåller en representation av bilden i det format som anges av typ‑parametern (standard är PNG). Den returnerade bilden har en upplösning på 96 dpi.

```java
public String ToDataURL(String type, double encoderOptions)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| typ | String | En String som indikerar bildformatet. Standardformatet är image/png. |
| encoderOptions | Double | Ett Number mellan 0 och 1 som indikerar bildkvaliteten om den begärda typen är image/jpeg eller image/webp. |

### Returvärde

En DOMString som innehåller den begärda data‑URI:n.

### Se även

* class [HTMLCanvasElement](../)
* package [com.aspose.html](../../../com.aspose.html/)
* package [Aspose.HTML](../../../)
