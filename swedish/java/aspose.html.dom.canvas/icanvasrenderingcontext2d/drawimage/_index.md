---
title: "ICanvasRenderingContext2D.DrawImage"
second_title: "Aspose.HTML för Java API-referens"
description: "ICanvasRenderingContext2D-metod. Ritar den angivna bilden."
type: docs

url: /sv/java/com.aspose.html.dom.canvas/icanvasrenderingcontext2d/drawimage/
---
## DrawImage(HTMLImageElement, double, double) {#drawimage_3}

Ritar den angivna bilden.

```java
public void DrawImage(HTMLImageElement image, double dx, double dy)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| bild | HTMLImageElement | HTMLImageElement att rita in i kontexten. |
| dx | Double | X‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dy | Double | Y‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |

### Se även

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double) {#drawimage}

Ritar den angivna bilden.

```java
public void DrawImage(HTMLCanvasElement image, double dx, double dy)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| bild | HTMLCanvasElement | HTMLCanvasElement att rita in i kontexten. |
| dx | Double | X‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dy | Double | Y‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |

### Se även

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLImageElement, double, double, double, double) {#drawimage_4}

Ritar den angivna bilden.

```java
public void DrawImage(HTMLImageElement image, double dx, double dy, double dw, double dh)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| bild | HTMLImageElement | HTMLImageElement att rita in i kontexten. |
| dx | Double | X‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dy | Double | Y‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dw | Double | Bredden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i bredd när den ritas. |
| dh | Double | Höjden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i höjd när den ritas. |

### Se även

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double, double, double) {#drawimage_1}

Ritar den angivna bilden.

```java
public void DrawImage(HTMLCanvasElement image, double dx, double dy, double dw, double dh)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| bild | HTMLCanvasElement | HTMLCanvasElement att rita in i kontexten. |
| dx | Double | X‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dy | Double | Y‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dw | Double | Bredden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i bredd när den ritas. |
| dh | Double | Höjden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i höjd när den ritas. |

### Se även

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLImageElement, double, double, double, double, double, double, double, double) {#drawimage_5}

Ritar den angivna bilden.

```java
public void DrawImage(HTMLImageElement image, double sx, double sy, double sw, double sh, 
    double dx, double dy, double dw, double dh)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| bild | HTMLImageElement | HTMLImageElement att rita in i kontexten. |
| sx | Double | X‑koordinaten för det övre vänstra hörnet av delrektangeln i källbilden som ska ritas in i destinationskontexten. |
| sy | Double | Y‑koordinaten för det övre vänstra hörnet av delrektangeln i källbilden som ska ritas in i destinationskontexten. |
| sw | Double | Bredden på delrektangeln i källbilden som ska ritas in i destinationskontexten. Om den inte anges används hela rektangeln från koordinaterna som specificerats av sx och sy till bildens nedre högra hörn. |
| sh | Double | Höjden på delrektangeln i källbilden som ska ritas in i destinationskontexten. |
| dx | Double | X‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dy | Double | Y‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dw | Double | Bredden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i bredd när den ritas. |
| dh | Double | Höjden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i höjd när den ritas. |

### Se även

* class [HTMLImageElement](../../../com.aspose.html/htmlimageelement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## DrawImage(HTMLCanvasElement, double, double, double, double, double, double, double, double) {#drawimage_2}

Ritar den angivna bilden.

```java
public void DrawImage(HTMLCanvasElement image, double sx, double sy, double sw, double sh, 
    double dx, double dy, double dw, double dh)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| bild | HTMLCanvasElement | HTMLCanvasElement att rita in i kontexten. |
| sx | Double | X‑koordinaten för det övre vänstra hörnet av delrektangeln i källbilden som ska ritas in i destinationskontexten. |
| sy | Double | Y‑koordinaten för det övre vänstra hörnet av delrektangeln i källbilden som ska ritas in i destinationskontexten. |
| sw | Double | Bredden på delrektangeln i källbilden som ska ritas in i destinationskontexten. Om den inte anges används hela rektangeln från koordinaterna som specificerats av sx och sy till bildens nedre högra hörn. |
| sh | Double | Höjden på delrektangeln i källbilden som ska ritas in i destinationskontexten. |
| dx | Double | X‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dy | Double | Y‑koordinaten i destination‑canvasen där det övre vänstra hörnet av källbilden ska placeras. |
| dw | Double | Bredden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i bredd när den ritas. |
| dh | Double | Höjden för att rita bilden i destination‑canvasen. Detta möjliggör skalning av den ritade bilden. Om den inte anges skalas inte bilden i höjd när den ritas. |

### Se även

* class [HTMLCanvasElement](../../../com.aspose.html/htmlcanvaselement/)
* interface [ICanvasRenderingContext2D](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
