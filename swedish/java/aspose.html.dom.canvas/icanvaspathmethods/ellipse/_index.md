---
title: "ICanvasPathMethods.Ellipse"
second_title: "Aspose.HTML för Java API-referens"
description: "ICanvasPathMethods‑metod. Lägger till en ellips i banan som är centrerad vid x‑y‑positionen med radierna radiusX och radiusY, startar vid startAngle och slutar vid endAngle och går i den angivna riktningen moturs (anticlockwise) som standard medurs (clockwise)."
type: docs

url: /sv/java/com.aspose.html.dom.canvas/icanvaspathmethods/ellipse/
---
## Ellipse(double, double, double, double, double, double, double) {#ellipse}

Lägger till en ellips i banan som är centrerad vid positionen (x, y) med radierna radiusX och radiusY, startar vid startAngle och slutar vid endAngle och går i den angivna riktningen moturs (standard är medurs).

```java
public void Ellipse(double x, double y, double radiusX, double radiusY, double rotation, 
    double startAngle, double endAngle)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| x | Double | x-axeln för koordinaten för ellipsens centrum. |
| y | Double | y-axeln för koordinaten för ellipsens centrum. |
| radiusX | Double | Ellipsens huvudaxelradie. |
| radiusY | Double | Ellipsens sekundäraxelradie. |
| rotation | Double | Rotationen för denna ellips, uttryckt i radianer. |
| startAngle | Double | Startpunkten, mätt från x-axeln, från vilken den kommer att ritas, uttryckt i radianer. |
| endAngle | Double | Slutellipsens vinkel som den kommer att ritas till, uttryckt i radianer. |

### Se även

* interface [ICanvasPathMethods](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)

---

## Ellipse(double, double, double, double, double, double, double, bool) {#ellipse_1}

Lägger till en ellips i banan som är centrerad vid positionen (x, y) med radierna radiusX och radiusY, startar vid startAngle och slutar vid endAngle och går i den angivna riktningen moturs (standard är medurs).

```java
public void Ellipse(double x, double y, double radiusX, double radiusY, double rotation, 
    double startAngle, double endAngle, bool anticlockwise)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| x | Double | x-axeln för koordinaten för ellipsens centrum. |
| y | Double | y-axeln för koordinaten för ellipsens centrum. |
| radiusX | Double | Ellipsens huvudaxelradie. |
| radiusY | Double | Ellipsens sekundäraxelradie. |
| rotation | Double | Rotationen för denna ellips, uttryckt i radianer. |
| startAngle | Double | Startpunkten, mätt från x-axeln, från vilken den kommer att ritas, uttryckt i radianer. |
| endAngle | Double | Slutellipsens vinkel som den kommer att ritas till, uttryckt i radianer. |
| anticlockwise | Boolean | Ett valfritt booleskt värde som, om sant, ritar ellipsen moturs (counter-clockwise), annars i medurs riktning. |

### Se även

* interface [ICanvasPathMethods](../)
* package [com.aspose.html.dom.canvas](../../../com.aspose.html.dom.canvas/)
* package [Aspose.HTML](../../../)
